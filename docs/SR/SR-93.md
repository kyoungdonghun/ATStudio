# SR-93 Toss Recurring Payment Production Readiness

> Status: OPEN for production readiness
> Created date: 2026-05-19
> Source REQ: REQ-20260519-ATS-001
> Implementation slice: REQ-20260521-ATS-001 applied on 2026-05-21
> Current implementation review: completed on 2026-05-23
> P1 production readiness slice: REQ-20260524-ATS-001 applied on 2026-05-24
> P1 incident workflow slice: REQ-20260524-ATS-002 applied on 2026-05-24
> P2 payment operations policy slice: REQ-20260525-ATS-002 applied on 2026-05-25
> P2-A audit/receipt evidence slice: REQ-20260525-ATS-003 applied on 2026-05-25
> P2-B admin refund ledger/provider cancel slice: REQ-20260525-ATS-004 applied on 2026-05-25
> P2-C refund entitlement correction slice: REQ-20260525-ATS-005 applied on 2026-05-25
> P2-D settlement import/reconciliation slice: REQ-20260526-ATS-001 applied on 2026-05-26
> P0 withdrawal billing-stop alignment: REQ-20260713-ATS-001 verified on 2026-07-13
> P1 payment-integrity code/test closure: WI-20260715-ATS-001 through WI-20260715-ATS-012 verified on 2026-07-15
> Settlement hardening update: WI-20260809-ATS-067 is complete; DG-067-09B is RUN-PASS-CLEANED historical evidence for the prior 42-table source snapshot on 2026-08-13
> Consent/session update: WI-20260809-ATS-068 completed focused repository scope on 2026-08-16; current source is 43 tables/entities and the recorded guarded disposable MySQL manifest is 43/511/175/91/6/6/0/0 with SHA-256 `b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`

## Purpose

This SR tracks the remaining production-readiness work for the Toss billing-key recurring subscription system.

The P0 withdrawal renewal-stop behavior is implemented and focused-test verified in implementation commit `d11c62d`. The later F-01 through F-05 payment-integrity code/test gate is also closed, as recorded in [P1 Payment Integrity Closure](../audit/p1-payment-integrity-closure-20260715.md). The 2026-07-17 local backend, frontend, schema, and tooling closeout remains historical evidence for that source snapshot. WI-067 holds the independently proven prior 42-table MySQL baseline and is `RUN-PASS-CLEANED`; the current 43-table source has a recorded guarded disposable manifest of 43 tables, 511 columns, 175 indexes, 91 foreign keys, 6 plans, 6 plan keys, zero forbidden tables/columns, and SHA-256 `b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`. This SR remains OPEN for production data strategy, live Toss validation, deployment and operational readiness, client acceptance, and explicit final release approval.

WI-20260817-ATS-014 upgraded the active frontend to pinned
`react-router-dom@7.18.2` and `react-router@7.18.2`, removing the two prior
moderate production audit findings. On 2026-08-17,
`npm audit --omit=dev --json` observed 0 vulnerabilities (0 info, 0 low,
0 moderate, 0 high, 0 critical) in that worktree. This dated audit result does
not establish a permanent audit state or close any SR-93 production gate.

## 2026-07-17 V1 Baseline Closeout

| Area | Official V1 baseline state |
|---|---|
| Account/security | Dual rate-limit budgets, ADMIN payment-role denial, trusted-proxy/JWT environment holds, and social-only withdrawal policy boundary are documented. |
| Payment integrity | Reconciliation uses bounded ID-keyset batches with full counters and capped issue details; billing keys use V2 key-ID envelopes; all payment cron jobs use a configurable zone with `Asia/Seoul` default. |
| Database baseline | Historical predecessor proof for the 2026-07-17 source snapshot: 41 tables, 493 columns, 168 indexes, 89 foreign keys, 6 seeded plans, SHA-256 `c581bef61cfba143744882b0674daf8d8fe742d82adbbf66d6b61699f5b86333`. It is not the current 43-source schema or a current required value. |
| WI-067 database proof | Historical prior-source proof: 42 tables, 506 columns, 173 index rows, 90 foreign keys, 6 plans/plan keys, zero forbidden tables/columns, and SHA-256 `acf28c935bf6107a8f2af431c971ebe0cd3539dba1aa1a941d966dde4a2a7a65`; independent Create/Validate, 3/3 MySQL concurrency tests under `ddl-auto=validate`, and exact cleanup passed. |
| Whitelist/certification/download | Transition, lock/version, immutable export, private-document audit, image canonicalization, and atomic download contracts from WI-007 through WI-009 are reflected in current design/client documents. |
| Frontend | Latest-request-wins, safe return targets, profile/store synchronization, modal lifecycle, and player retry/stall/accessibility behavior from WI-010 are current. |
| Dependency audit | The Router 6.30.4/Vite 6.4.3 audit evidence in the 2026-07-17 baseline is historical. The current Router 7.18.2 remediation and its dated `npm audit --omit=dev --json` observation are recorded above; they removed the two prior moderate findings but do not close production readiness. [WI-20260724-ATS-021](../../deliverables/agent/WI-20260724-ATS-021-evidence-pack.md) remains pre-upgrade reachability evidence only. |
| Backend quality | 1,208 tests, 0 failures/errors, and 9 environment-dependent skips. JaCoCo: instruction 85.673%, branch 71.682%, line 85.726%, method 82.931%. |
| Frontend quality | 468 tests and 0 failures. Coverage: statements 86.73%, branches 76.98%, functions 85.41%, lines 88.75%. |
| Tooling gates | Frontend typecheck, ESLint, Prettier, and build all PASS. |

The 2026-07-17 figures and WI-067 proof preserve historical source snapshots.
Current source has 43 tables/entities and a recorded guarded disposable MySQL
manifest of 43 tables, 511 columns, 175 indexes, 91 foreign keys, 6 plans, 6
plan keys, zero forbidden tables/columns, and SHA-256
`b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`.
None of these results proves a retained-data migration, live
Toss behavior, production deployment or monitoring, client acceptance, or final
release approval.

## Remaining Production Gates

- Confirm the production data strategy. V1 supports a verified-empty database initialized from `schema.sql` and `seed.sql`; retaining existing data requires a separate approved migration design and rehearsal.
- Configure production secrets and complete live Toss staging/production rehearsals for charge, renewal, reconciliation, refund, and billing-key cleanup behavior.
- Verify the production HTTPS/proxy/CORS topology, secret management, backup/restore, scheduler ownership, logs, alerts, and incident response.
- Complete client acceptance against a newly verified operator-controlled environment.
- Record explicit production and final release approval. This SR remains OPEN until those gates are evidenced.

## 2026-09-05 Local Verification

Source: MA's dated execution report in
[WI-003](../../deliverables/agent/WI-20260905-ATS-003-evidence-pack.md#2026-09-05-ma-results-received)
and the exact Origin comparison/browser result in
[WI-005](../../deliverables/agent/WI-20260905-ATS-005-evidence-pack.md).
The runtime belongs to the main development checkout on
`codex/v1-release-rehearsal-fixes`, `69d0226` plus working-tree changes. These
are local observations, with initial and final suite results separated below;
no release commit or production GO is established. MA supplied final build,
coverage and static-check PASS results after WI-004.
[WI-002 runtime evidence](../../deliverables/agent/WI-20260905-ATS-002-evidence-pack.md)
is complete. On 2026-09-05, MA reported document validation PASS (665 IDs,
links and index) and `git diff --check` PASS. MA verified the owned backend
30612 and frontend 28724 stopped and their ports released; these are completed
observations, not currently running services. Only scoped staging and commit
remain MA-owned for the local closeout. The reported source/UI freeze is
16:36:50; the earlier desktop report recorded no HMR since 16:35:23.

| Area | MA-reported result | Boundary |
|---|---|---|
| Initial backend quality | `gradlew.bat build --rerun-tasks` PASS; 1,632 tests, 0 failures, 0 errors, 19 skipped; JaCoCo gates PASS. | Preserved initial snapshot; the final rerun is recorded separately below. |
| Initial frontend quality | Coverage PASS: 111 files, 1,449 tests; statements 90.01%, branches 82.29%, functions 90.85%, lines 92.56%. Typecheck, lint, full formatting and build PASS. | Initial source snapshot, not a blanket final acceptance result. |
| WI-004 focused verification | MA reports WI-004 complete with 84 frontend and 107 backend focused tests passing. | Focused evidence; not substituted for the final full-suite results. |
| Final backend quality | Build PASS in 2m55s. JUnit: 1,689 total, 1,670 executed, 0 failures, 0 errors, 19 skipped. JaCoCo line 87.374%, method 85.261%, branch 72.338%; gates PASS. | Skips: 18 gated MySQL tests and 1 LocalStorage platform skip. Those 19 were not executed; no new live MySQL proof is implied. |
| Final frontend quality | 112 files / 1,458 tests PASS in 234.78s. Coverage: statements 90.1%, lines 92.66%, functions 91.04%, branches 82.38%. Final typecheck, lint, full formatting and build PASS. | Final MA quality result, not production deployment or external-provider acceptance. |
| Operation helpers | `test-dry-run.ps1`, `test-backend-environment.ps1` (10 checks), and `test-bootstrap-guards.ps1` PASS; DB Preflight source count 43 PASS. | Synthetic/source guards, no current Create/Observe/Drop/restore or new database. |
| Existing development runtime | Read-only counts: 43 tables, 511 columns, 175 distinct indexes, 232 index-column entries, 91 foreign-key columns and 6 plans. Journal remained 30 DONE; QA bootstrap off. | MA separately confirmed 175 distinct indexes. The 232 index-column entries are a different unit, not a mismatch. No canonical manifest regeneration, backup/restore or scheduler rehearsal is claimed. |
| Rebuilt backend restart | MA restarted only owned backend PID 29008; rebuilt backend PID 30612 started in 20 seconds in the main worktree, local profile, absolute storage roots and QA bootstrap off. Hibernate validate PASS; integrity remained 30 checked / 20 available / 10 missing. | Dated process identity, not permanent ownership or strict production-integrity PASS. Missing historical references were not repaired. |
| Read-only nickname comparison | Actual profile confirmed ADMIN role. Existing canonical nickname and its NBSP/BOM-wrapped form both returned unavailable (`false`); comparison PASS. MA logged out afterward. | No account-field mutation; no nickname or token value is published. |
| Storage/media | Non-strict local audit still reports 30 references/10 missing. All 10 current demo Tracks (IDs 4-13) returned full-stream 200, range 206 and thumbnail 200. ADMIN integrity inspection 200, ordinary-user 403 and anonymous 401. | Current demo media and access boundaries pass; historical missing references remain unchanged. No repair or strict production-integrity PASS is claimed. |
| Origin and single-track reload | Public batch POST with localhost Origin returned 200; 127.0.0.1 Origin returned 403 `Invalid CORS request`; an untrusted Origin was rejected with 403. At `http://localhost:5173/tracks/4`, guest play/pause/seek to 5 seconds/reload retained Track 4, 5 seconds, paused state and the green waveform. | Controlled POST comparison and actual browser observations are separate evidence. No player code fix or CORS expansion was needed. |
| Visible-list automatic next | At canonical localhost with repeat off, Track 9 naturally advanced to 10, 11 and 12; MA paused Track 12 at 4.26 seconds, all healthy. | PASS for the observed natural list progression, not every repeat mode. |
| Repeat-all at visible-list end | With repeat-all on, last visible Track 7 ended paused on Track 7; no wrap occurred. | Existing [SR-83](SR-83.md) visible-list priority stops at the end before queue-repeat logic. This is a maintenance/policy-clarification item: the repeat-all label can imply list wrapping. No new regression or code/policy change is claimed; visible-list repeat-all wrap is NOT PASS. |
| Queue repeat-all outside visible list | After the Track 7 end, MA navigated Home to clear page context, resumed, and observed the queue wrap to Track 4 (`DawnSignal`) playing, then paused at 1 second. | PASS for actual queue repeat-all outside visible-list context; it does not alter the intentional visible-list stop. |
| Final guest UI checks | Likes parent -> Playlists child -> Likes parent kept the drawer open with Likes pressed; another Likes parent click closed it. Calm and Bright mood chips both remained visible/pressed with zero AND results. Home displayed the updated creator copy/footer. | MA-observed browser behavior, recorded separately from the final full-suite results. |
| Mobile browser | At 390x844, expanded-player screenshot showed buttons, waveform and volume without overlap. Volume Home/End interaction ended at 1; Likes manual-tab/parent-click behavior passed. Viewport was reset afterward. | Actual MA screenshot/interaction evidence at this viewport, not a claim for every device or a substitute for full tests. |

No Cloudflare, provider/payment/refund, external mail, new database, historical
asset repair, backup/restore or production deployment was performed in this
local closeout report. The [browser-origin preflight](../design/runtime-storage-operations.md#browser-origin-preflight)
uses the documented localhost origin; a no-Origin HTTP 200 is not browser proof.

The external and target-dependent gates above remain OPEN, including actual
single-server scheduler ownership/jobs, log collection and chosen alert/incident
response, recoverability, deployment configuration, approved Toss/SMTP exercises,
client acceptance and explicit GO. Retained-data migration is conditional on a
real preservation requirement; this closeout does not require another DB or
repairing throwaway tracks. A tuple with missing references still cannot count
as a healthy strict production tuple. Multi-server locking and additional
payment/notification channels remain outside the current V1 scope.

## 2026-07-15 Payment Integrity Update

| Area | Current verified state |
|---|---|
| Stable renewal command | One unresolved billing period retains one order and command; retries consume the due gate and use a new attempt identity on the same command. |
| Provider transaction boundary | Payment/refund provider calls covered by the remediation run outside local transactions and fail on an active caller transaction. |
| Refund recovery | Refund execution uses one row/key, a 15-minute processing lease, stale-result fencing, and Incident-backed lookup-only fallback after the replay ceiling. |
| Reconciliation | Exact provider evidence may enter `PROVIDER_SUCCEEDED` and dispatch purpose-specific finalize-only recovery; mismatches remain Incident-only. |
| Sensitive transaction evidence | Exact provider transaction ownership remains in protected structured fields; serialized lookup evidence and Incident/audit text omit or mask the raw payment key. |
| MySQL proof | The predecessor source snapshot remains historical. WI-067 records the historical 42/506/173/90/6 manifest and passed independent validation plus 3/3 settlement concurrency tests; both exact disposable databases were dropped. Current 43-table source has its own recorded guarded disposable manifest: 43/511/175/91/6/6/0/0 with SHA-256 `b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`. |

This update does not verify a retained database, production migration, live Toss,
production deployment, or client acceptance. The historical WI-009/WI-010 FAIL
reviews remain preserved; WI-011 corrected their findings and WI-012 returned
PASS for that follow-up scope.

## Current Implementation Snapshot

### Implemented

| Area | Current state |
|---|---|
| New subscription checkout | User-facing subscription purchase enters `/subscriptions/checkout` and uses Toss billing auth. |
| Billing auth callback | `/subscriptions/checkout/success` and `/subscriptions/checkout/fail` are the active callback routes. |
| First charge | Billing key confirmation immediately performs the first subscription charge before activating the subscription. |
| Payment command integrity | New payment commands persist stable command and attempt identities before Provider mutation; provider success is durable before local finalization. |
| Removed subscription compatibility paths | Direct subscription creation, legacy one-time subscription prepare/confirm APIs, and stale payment callback routes are absent. Recurring billing agreement endpoints are the only V1 subscription payment contract. |
| Upgrade | Plan upgrade is confirmed from the subscription management preview and charged through the active billing agreement. |
| Upgrade plus next-cycle change | The higher plan applies immediately after the upgrade charge. If the user selected a different next renewal cycle, only the billing-cycle change remains pending and must be labeled separately from a pending plan change. |
| Removed billing-key recovery | If Toss reports a stored billing key as removed/invalid during upgrade, the local billing agreement is marked `EXPIRED`, issued-key metadata is cleared, and the subscription remains unchanged. |
| Payment method re-registration | Active or CANCELLED grace-period subscribers can register a new payment method through a zero-amount `BILLING_AGREEMENT` order without changing the current subscription. Interrupted `READY` registrations are treated as incomplete and can be restarted. Upgrade-origin registrations preserve return context so the user can continue the selected plan change after card registration. |
| Upgrade billing cycle rule | Upgrade preserves the current paid period and current billing cycle. If the user selected a different next cycle, that cycle is stored as pending for the next renewal. |
| Scheduled change | Lower-tier and billing-cycle-only changes are payment-free and stored as a pending plan/cycle change. A later selection can overwrite the pending change. |
| Pending change clear | Selecting the current plan/current cycle clears a pending plan/cycle change. |
| Automatic renewal | `RecurringRenewalService` charges due active billing agreements through bounded per-command phases and extends subscriptions on success. |
| Renewal failure policy | Renewal failure uses a 3-day grace period and up to 3 attempts on the same billing-period command. A claimed retry consumes its scheduling gate. Failure emails are sent through the existing email service. |
| Subscription cancel | User-facing cancellation stops the next renewal by marking the subscription and local billing agreement as cancelled while preserving already-paid access until `expiresAt`. |
| Account withdrawal billing stop | Password-authenticated withdrawal marks a non-terminal agreement and ACTIVE subscription `CANCELLED` before soft-deleting the user. Deleted users are excluded by both renewal query and service guard. |
| Withdrawal Provider cleanup | An ID-only event runs after commit; agreement-specific `REQUIRES_NEW` cleanup clears issued-key fields on Provider success. |
| Withdrawal cleanup failure/retry | Failure keeps local cancellation and encrypted key material, creates/increments an agreement-scoped `WARNING` `LOCAL_DONE_PROVIDER_NOT_DONE` Incident, and is retried daily at 01:15. |
| Already-removed convergence | `ALREADY_REMOVED_BILLING_KEY` is treated as successful cleanup, clears local key material, and resolves the matching Incident. |
| Withdrawal refund boundary | Account withdrawal creates no automatic refund. Refund remains a separate admin request/approval/execution workflow. |
| Cancellation reactivation | A cancelled grace-period subscription can be reactivated before `expiresAt` by reusing the stored billing key when the billing agreement is still valid. |
| Provider-level billing agreement cancel | The legacy/operations billing-agreement cancel API deletes the provider billing key and clears local issued-key fields; it is not the current user-facing cancel UX. |
| Stale order cleanup | Scheduler expires old `READY` and `IN_PROGRESS` payment orders. |
| Local ledger reconciliation | `PaymentReconciliationService` logs local mismatches such as `DONE` order without payment row and active billing agreement without active subscription. |
| Provider API reconciliation | `PaymentReconciliationService` scans every eligible Toss billing payment order in bounded ascending-ID keyset batches and compares provider state by `orderId`; full mismatch counters are separate from capped returned issue details. Exact evidence may resume finalize-only local completion, while contradictions remain Incident-only. |
| Admin visibility | `/admin/payments` provides payment order, billing agreement, subscription payment, reconciliation incident, receipt, audit, settlement, refund, and entitlement correction operation tabs. |
| Admin reconciliation | `GET /api/admin/payments/reconciliation` runs read-only local/provider reconciliation and returns support-safe issue records. |
| Reconciliation incident storage | Scheduled reconciliation creates or updates `payment_reconciliation_incidents` with `OPEN`, `ACKNOWLEDGED`, `RESOLVED`, and `IGNORED` workflow states. |
| Reconciliation incident admin APIs/UI | Admins can list incidents and update incident workflow state from APIs and `/admin/payments` without mutating payments, subscriptions, or billing agreements. |
| Reconciliation operator notification | Optional operator email notification is available when `PAYMENT_RECONCILIATION_NOTIFICATION_ENABLED=true` and `PAYMENT_OPERATIONS_OPERATOR_EMAIL` is set. |
| Receipt evidence storage | Successful initial subscription, upgrade, and renewal charges publish safe receipt evidence after commit. `payment_receipts` stores provider receipt URL/key metadata when Toss returns it. |
| Payment operation audit logs | `payment_operation_audit_logs` stores reconciliation incident status changes, system receipt evidence creation events, admin refund workflow transitions, and admin entitlement correction workflow transitions. |
| Refund ledger and Toss cancel API | Admin refund preview/request/approve/execute/read APIs are implemented through `payment_refunds`. Provider cancel execution uses the persisted idempotency key, a fenced processing lease, and strict no-transaction invocation; it does not automatically mutate subscription entitlement. |
| Refund entitlement correction | Admin entitlement correction preview/request/approve/execute/read APIs are implemented through `payment_entitlement_corrections`. Execution applies only explicit target subscription state and optionally local billing agreement cancellation; it does not call provider billing-key delete APIs or infer previous-plan rollback. |
| Settlement import/reconciliation | Admin strict UTF-8 CSV import, durable attempt/recovery, bounded error response, settlement row list, maximum-90-day/5,000-row missing-provider scan, and ignore workflow are implemented. Settlement rows are accounting review evidence and do not mutate subscriptions, payments, refunds, billing agreements, or provider state. |
| Refund/settlement/tax invoice policy | Policy is documented in [Payment Refund, Receipt, Settlement, and Tax Invoice Policy](../design/payment-refund-receipt-settlement-policy.md). First-class admin receipt/audit/settlement/refund/entitlement UI is implemented. Tax invoice workflow and cash receipt issue/cancel automation are on hold for card-only recurring billing. |
| Sensitive data boundary | User/admin responses do not expose raw billing keys, auth keys, Toss secret keys, raw card data, raw provider payloads, or exact provider payment/refund/receipt/settlement identifiers. ADMIN screens use deterministic `REF-*` support references; Incident/audit free text never carries the raw value. |

### Retired or absent

| Item | Current decision |
|---|---|
| SR-92 inline one-time widget UX | Retired for subscription scope. Recurring billing auth replaced the one-time widget flow. |
| QR or one-time subscription purchase | Removed from user-facing subscription flow. |
| Direct mock-style subscription creation | Removed from production code. |
| Legacy subscription payment APIs and callback routes | Removed from the backend/frontend contract; they are not V1 compatibility paths. |
| Removed payment provider switch | `app.payment.provider`, `APP_PAYMENT_PROVIDER`, `TOSS_CONFIRM_URL`, and the legacy one-time payment provider path are absent from the V1 configuration and runtime contract. The acceptance launcher rejects these obsolete bundle names instead of preserving compatibility aliases. |

## Production Transition Checklist

### Configuration

- Provision Toss test and live keys separately.
- Use the current [Acceptance Environment Operator Guide](../../scripts/acceptance/README.md) for the Cloudflare prerequisite, external JSON allowlist, secret-file ACL, fixed ports, and start/status/stop lifecycle. Obsolete payment environment names are rejected rather than translated.
- Set the V2 billing-key active ID and key ring only through environment variables or a secret manager.
- Configure billing auth success/fail URLs to the production frontend origin.
- Confirm `TOSS_PAYMENT_LOOKUP_BY_ORDER_ID_URL` uses the production Toss payment lookup endpoint.
- Confirm production CORS allows only intended frontend origins.
- Keep `application-local.yml` out of production deployment assumptions.
- Confirm recurring checkout fails safely with `PAYMENT_PROVIDER_NOT_CONFIGURED` when Toss billing keys are missing.

### Security and data handling

- Never store raw card numbers, CVC, or expiry data.
- Never return raw billing keys to frontend or admin APIs.
- Keep Toss secret keys server-side only.
- Keep `authKey` and `customerKey` limited to the billing auth confirmation boundary.
- Sanitize provider failure payloads before storing or displaying them.
- Check logs for accidental raw provider payload, billing key, or card data exposure.

### Operations

- Run only one renewal scheduler instance in the current single-server deployment.
- Keep renewal charge idempotency based on `PaymentOrder` and provider idempotency key.
- Monitor failed renewal email delivery.
- Monitor scheduler logs for stale order expiration and renewal failures.
- Monitor retry claims that remain unresolved after the scheduling gate is consumed; do not create a new logical renewal command.
- Monitor local and provider reconciliation warnings.
- Treat `PROVIDER_SUCCEEDED` as finalize-only and preserve the open Incident if local finalization fails.
- Treat stale refund `PROCESSING` rows as lease/reconciliation cases; never create a replacement refund row or idempotency key.
- Monitor the 01:15 withdrawal cleanup retry aggregate and agreement-scoped cleanup Incidents.
- Confirm WARN-level reconciliation logs are collected by the production log monitoring system.
- Use `GET /api/admin/payments/reconciliation` for on-demand mismatch checks.
- Use `/admin/payments` or `GET /api/admin/payments/reconciliation-incidents?status=OPEN` for persisted incident triage.
- Enable `PAYMENT_RECONCILIATION_NOTIFICATION_ENABLED` and `PAYMENT_OPERATIONS_OPERATOR_EMAIL` if operator email alerts are required.
- Use `GET /api/admin/payments/receipts` to confirm safe receipt evidence after successful staging charges when provider receipt fields are present.
- Use `GET /api/admin/payments/operation-audit-logs` to confirm incident status changes and receipt evidence creation were recorded.
- Use `GET /api/admin/payments/refund-preview/{subscriptionPaymentId}` and the refund request/approve/execute APIs only for support-approved refund cases in a safe test/staging environment.
- Use `POST /api/admin/payments/entitlement-correction-preview` and the entitlement correction request/approve/execute APIs only after a refund has succeeded and support has approved the exact target access state.
- Use `GET /api/admin/payments/settlements`, `POST /api/admin/payments/settlements/import`, `POST /api/admin/payments/settlements/reconcile`, and `PUT /api/admin/payments/settlements/{settlementId}/ignore` only for PG-to-ATStudio settlement review. These APIs must not be used as payment/refund/subscription mutation tools.
- Use [Payment Operations Runbook](../design/payment-operations-runbook.md) for provider success plus local persistence failure.
- For withdrawal cleanup failure, keep the user deleted and agreement `CANCELLED`; do not restore renewal eligibility or manually clear encrypted key material. Allow targeted retry and verify automatic Incident resolution after convergence.
- Do not create a refund solely because account withdrawal occurred.
> Current deployment assumes a single application server. Multi-server scheduler lock is intentionally out of active scope and should be reconsidered only before running more than one application instance.

### Database and production data strategy

- Confirm `billing_agreements`, `payment_orders`, `subscription_payments`, `payment_refunds`, `payment_entitlement_corrections`, `payment_settlement_import_attempts`, `payment_settlements`, `payment_reconciliation_incidents`, `payment_receipts`, and `payment_operation_audit_logs` schema exists in production.
- Confirm indexes for billing agreement status/next billing date and payment order lookup exist.
- For local rehearsal, use the guarded [Disposable MySQL Bootstrap](../../scripts/database/README.md). It accepts loopback only, requires an exact disposable name, applies only current `schema.sql` then `seed.sql`, and never supplies a retained-data upgrade path.
- For the fresh-only V1 path, start from a verified-empty MySQL 8 database, apply `schema.sql` and `seed.sql` once, and require Hibernate `ddl-auto=validate` to pass.
- Verify current source preflight derives exactly 43 `CREATE TABLE` statements
  and reports `mysql.manifest.expectation=RECORDED`. The recorded current
  disposable manifest is 43 tables, 511 columns, 175 indexes, 91 foreign keys,
  6 plans, 6 plan keys, zero forbidden tables/columns, and SHA-256
  `b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`.
  The 42-table, 506-column, 173-index-row, 90-foreign-key hash is WI-067
  historical evidence only, not a current requirement. Any future disposable
  run requires new immediate approval and new exact names.
- Do not use deleted manual SQL files. This repository does not supply an upgrade path for a retained database.
- If production must retain existing data, create and approve a separate migration requirement, design, backup, rehearsal, and rollback plan before deployment.
- Back up subscription and payment tables before production activation.
- Decide whether existing local/test subscriptions should be expired, cancelled, or left as test data before production-like staging.

## Manual Acceptance Checklist

Use this list for staging or production rehearsal.

| Scenario | Expected result |
|---|---|
| New subscription success | Billing auth succeeds, first charge succeeds, subscription becomes `ACTIVE`. |
| Billing auth failure | No subscription is created or changed. User can retry with a fresh checkout. |
| First charge failure after billing key issue | Subscription is not activated, order is failed, issued key is cancelled where possible. |
| Recurring-only checkout surface | Current frontend and backend inventories contain only billing-agreement recurring subscription checkout paths; removed direct/one-time APIs and stale callback routes are absent. |
| Upgrade with active billing agreement | Remaining-period difference is charged immediately, plan changes immediately, current expiration date remains. |
| Upgrade with provider-removed billing key | Charge fails with `BILLING_AGREEMENT_REAUTH_REQUIRED`, billing agreement becomes `EXPIRED`, issued-key metadata is cleared, subscription stays unchanged. |
| Interrupted payment method registration | Billing agreement status `READY` is shown as incomplete, payment method is not displayed as registered, and the user can restart registration. |
| Upgrade-origin payment method registration | Checkout shows that registration itself has no immediate charge, separately shows the selected upgrade's expected prorated amount, and returns to the selected plan/cycle preview after registration succeeds. |
| Payment method re-registration | Active subscriber completes Toss billing auth with `purpose=BILLING_AGREEMENT`; no immediate charge occurs, billing agreement becomes `ACTIVE`, next billing date remains the subscription `expiresAt`. |
| Upgrade with selected next cycle different from current cycle | Upgrade charge applies the higher plan immediately. Current paid cycle remains until renewal, selected next cycle is stored as pending, and the UI labels only the billing-cycle reservation rather than implying the upgraded plan is still pending. |
| Upgrade without active billing agreement | No one-time checkout fallback. User sees safe guidance. |
| Scheduled lower-tier/cycle change | No immediate charge. Pending change applies after the next successful renewal. |
| Pending change overwrite | User can choose another plan/cycle before renewal; pending values are replaced. |
| Pending change clear | User selects the current plan/current cycle; pending values are cleared. |
| Automatic renewal success | Renewal charge succeeds, subscription extends, failure count resets. |
| Automatic renewal failure during grace | Failure count increases, email is sent, access remains during grace. |
| Automatic renewal retry | The same billing-period order remains visible with a later attempt; a second finalized payment is not created. |
| Automatic renewal final failure | Billing agreement becomes suspended and subscription expires after grace. |
| Subscription cancel | Future charges stop, current paid access remains until `expiresAt`. |
| Cancellation reactivation | Before `expiresAt`, user can keep the subscription and future renewal resumes from the original expiresAt without card re-registration when the stored billing key is valid. |
| Account withdrawal | User, ACTIVE subscription, and non-terminal agreement become locally non-renewable before cleanup dispatch; no refund row is created. |
| Renewal after withdrawal | Due renewal produces zero key-decrypt, order-save, subscription-lookup, and Provider-charge calls for the deleted user. |
| Withdrawal cleanup failure | Local cancellation remains; encrypted key is retained; one agreement-scoped `WARNING` Incident is created or incremented. |
| Withdrawal cleanup retry success | Issued-key fields are cleared and the matching Incident is resolved. |
| Provider key already removed | `ALREADY_REMOVED_BILLING_KEY` converges to the same cleared/resolved result. |
| Admin read-only payment page | Shows operational records without raw sensitive data. |
| Admin reconciliation endpoint | Shows local/provider mismatch counts and issue records without raw sensitive data. |
| Scheduled reconciliation incident storage | Detected mismatches create or update `payment_reconciliation_incidents`; repeated detections increment occurrence metadata. |
| Admin incident workflow | Admin can list incidents and change status to `ACKNOWLEDGED`, `RESOLVED`, or `IGNORED` from `/admin/payments` without mutating subscription/payment state. |
| Payment receipt evidence | After a successful staging charge, `GET /api/admin/payments/receipts` returns provider receipt evidence if Toss returned receipt fields, without raw card/provider payload data. |
| Payment operation audit log | After an admin incident status change, `GET /api/admin/payments/operation-audit-logs` returns the before/after status audit row. |
| Refund preview | For a refundable Toss billing subscription payment, `GET /api/admin/payments/refund-preview/{subscriptionPaymentId}` returns original, reserved, and remaining refundable amounts. |
| Refund request/approval | Admin creates and approves a refund request; `payment_refunds` and `payment_operation_audit_logs` record the workflow without calling Toss before execution. |
| Refund execution | Admin executes an approved refund after typed confirmation; Toss cancel is called with the stored idempotency key, the refund becomes `SUCCEEDED`, `FAILED`, or `PENDING_PROVIDER_CONFIRMATION`, and subscription entitlement is not automatically changed. |
| Repeated/stale refund execution | The same refund row remains visible; an in-progress or ambiguous result is not bypassed with a replacement request. |
| Entitlement correction preview/request | After a succeeded refund, admin previews and creates an explicit target subscription state; `payment_entitlement_corrections` stores before/target snapshots without mutating access yet. |
| Entitlement correction approval/execution | Admin approves and executes the correction after typed confirmation; `user_subscriptions` is updated to the explicit target state, optional local billing agreement cancellation is applied, and `payment_operation_audit_logs` records the workflow. |
| Settlement CSV import | Admin imports a valid settlement CSV; `payment_settlements` rows are created with `MATCHED`, `MISMATCHED`, or `LOCAL_PAYMENT_NOT_FOUND` status and no subscription/payment/refund/provider state changes. |
| Settlement missing-provider scan | Admin runs settlement reconcile for a selected period; local finalized payments without imported provider evidence create `PROVIDER_SETTLEMENT_NOT_FOUND` review rows unless provider evidence already exists. |
| Settlement ignore workflow | Admin ignores a verified settlement row with a note; the row becomes `IGNORED`, an audit row is recorded, and the original row remains queryable. |

## Remaining Development Points

These are not blockers for closing the current local/test payment feature. They are production operation items and should be split into future REQ/SR work.

| Priority | Item | Why it remains |
|---|---|---|
| P2 | Slack/SMS/in-app operator notifications | Email notification is optional; additional notification channels are future operations integrations. |
| P2 | Toss Settlement API adapter automation | CSV/manual settlement import is implemented first; direct provider settlement API import remains future if operations volume requires it. |
| P3 | Multi-PG adapters | KakaoPay, Naver Pay, or additional PGs remain future provider-extension work. |
| Conditional | Tax invoice request workflow | On hold while ATStudio remains card-only recurring billing. Reopen only for approved B2B invoice, bank-transfer, postpaid, contract purchase, or institution purchase scope. |

Conditional infrastructure item: multi-server scheduler lock remains excluded while ATStudio runs as a single application server. Reopen it only before more than one application instance can execute renewal/reconciliation schedulers.
Cash receipt issue/cancel automation is excluded from the current next-step list while recurring billing remains card-only. Reopen it only if a cash-like payment method or a separate cash receipt request flow is approved.

## Close Criteria for Payment Feature Slice

The payment feature slice can be considered closed when the following remain true:

- User-facing subscription purchase uses recurring billing only.
- First subscription activation happens only after billing key issue and first charge success.
- Upgrade uses the active billing agreement instead of one-time checkout.
- Upgrade plus next-cycle selection shows the upgraded plan as active immediately and only the next billing cycle as pending.
- Scheduled lower-tier/cycle changes are pending-only and payment-free until renewal.
- Pending changes can be overwritten or cleared before renewal.
- User-facing cancellation can be reversed before expiresAt when the stored billing key remains valid.
- Account withdrawal stops local renewal before soft deletion, retries Provider cleanup without restoring charge eligibility, and never auto-refunds.
- Deleted, expired, or interrupted billing-key registrations can be recovered through zero-amount payment-method re-registration.
- Direct/mock-style subscription creation is absent.
- Local/test validation passes across backend tests, frontend tests, typecheck, lint, build, and docs validation.
- Payment-integrity code/test closure remains linked to reproducible WI. The
  predecessor fresh-MySQL proof and WI-067 42-table manifest/concurrency proof
  are historical evidence. Current 43-table source has a recorded guarded
  disposable MySQL manifest of 43/511/175/91/6/6/0/0 with SHA-256
  `b177b34780fabc75ea8b4608a0d210167a81d414d2778cc1d1dc5c0e39c8fea4`;
  neither state claims retained-data migration or live-provider validation.
- Production-specific items above are tracked as SR-93 or future REQ/SR follow-up work.

## Completed P1 Production Readiness Items

| Item | Status |
|---|---|
| Provider API reconciliation | Implemented through Toss payment lookup by `orderId`; webhook remains optional follow-up. |
| Provider success plus local persistence failure runbook | Documented in [Payment Operations Runbook](../design/payment-operations-runbook.md). |
| Production Toss configuration checklist | Updated in this SR and the runbook. |
| Persistent reconciliation incident storage | Implemented through `payment_reconciliation_incidents` with deterministic dedupe and workflow states. |
| Admin reconciliation incident frontend UI | Implemented in the `/admin/payments` incident tab. |
| Admin receipt/audit/refund/entitlement frontend UI | Implemented in `/admin/payments` with separate tabs, typed confirmation for destructive execution, and an explicit boundary from ordinary user-subscription administration. |
| Operator notification | Optional email notification is implemented behind explicit operations configuration. |
| Operational incident response | Documented with support-safe evidence boundaries and incident status workflow. |

## Completed P2 Policy Items

| Item | Status |
|---|---|
| Refund policy | Documented as admin-only, audited, idempotent, and separate from user-facing subscription cancellation. |
| Receipt/cash receipt policy | Documented with provider receipt URL, card-first recurring billing, and cash receipt automation boundary. |
| Settlement policy | Documented as PG-to-ATStudio merchant settlement first; creator payout remains out of scope. |
| Tax invoice policy | Documented as on hold for current card-only recurring billing; future manual HomeTax/ASP-backed operations tracking requires B2B/non-card payment scope and tax review. |
| Future data/API candidates | Documented for Toss Settlement API adapter automation and additional admin action audit values. Tax invoice and cash receipt mutation remain conditional on future B2B/non-card or cash-like payment support. |

## Completed P2-A Audit/Receipt Foundation Items

| Item | Status |
|---|---|
| Receipt evidence ledger | Implemented through `payment_receipts`; rows are created after successful committed charges when safe provider receipt fields exist. |
| Receipt evidence admin API | Implemented through `GET /api/admin/payments/receipts`. |
| Payment operation audit ledger | Implemented through `payment_operation_audit_logs`. |
| Audit admin API | Implemented through `GET /api/admin/payments/operation-audit-logs`. |
| Incident status audit | `PUT /api/admin/payments/reconciliation-incidents/{incidentId}/status` records before/after status, actor, note, and target linkage. |
| Sensitive evidence boundary | Receipt evidence uses minimal sanitized metadata only; raw provider payloads, billing keys, auth keys, customer keys, and raw card data remain forbidden. |

## Completed P2-B Refund Ledger/Provider Cancel Items

| Item | Status |
|---|---|
| Refund ledger | Implemented through `payment_refunds` with request, approval, processing, success, failure, and pending-provider-confirmation states. |
| Refund preview API | Implemented through `GET /api/admin/payments/refund-preview/{subscriptionPaymentId}`. |
| Refund read APIs | Implemented through `GET /api/admin/payments/refunds` and `GET /api/admin/payments/refunds/{refundId}`. |
| Refund mutation APIs | Implemented through `POST /api/admin/payments/refunds`, `POST /api/admin/payments/refunds/{refundId}/approve`, and `POST /api/admin/payments/refunds/{refundId}/execute`. |
| Toss cancel integration | Implemented through provider-side payment cancel call with Basic auth and a persisted `Idempotency-Key`. |
| Refund audit | Refund workflow transitions create `payment_operation_audit_logs` rows. |
| Entitlement boundary | Refund execution does not automatically change `user_subscriptions`, `billing_agreements`, or `subscription_payments.payment_status`; entitlement correction is handled by the separate P2-C workflow below. |

## Completed P2-C Refund Entitlement Correction Items

| Item | Status |
|---|---|
| Entitlement correction ledger | Implemented through `payment_entitlement_corrections` with before/target snapshots and request/approval/processing/success states. |
| Entitlement correction preview API | Implemented through `POST /api/admin/payments/entitlement-correction-preview`. |
| Entitlement correction read APIs | Implemented through `GET /api/admin/payments/entitlement-corrections` and `GET /api/admin/payments/entitlement-corrections/{correctionId}`. |
| Entitlement correction mutation APIs | Implemented through `POST /api/admin/payments/entitlement-corrections`, `POST /api/admin/payments/entitlement-corrections/{correctionId}/approve`, and `POST /api/admin/payments/entitlement-corrections/{correctionId}/execute`. |
| Explicit target policy | Execution applies only the admin-provided target plan, billing cycle, status, expiration date, pending clear option, and local billing agreement cancellation option. Previous-plan rollback is not inferred from payment history. |
| Provider boundary | Local billing agreement cancellation is local-only. Provider billing-key delete/cancel API is not called by entitlement correction. |
| Entitlement audit | Entitlement correction workflow transitions create `payment_operation_audit_logs` rows. |

## Completed P2-D Settlement Import/Reconciliation Items

| Item | Status |
|---|---|
| Settlement ledger | Implemented through `payment_settlements` with CSV/manual import rows and generated missing-provider review rows. |
| Settlement import API | Implemented through `POST /api/admin/payments/settlements/import`; accepts a required multipart file and optional multipart note with header-only Idempotency-Key, enforces the 5 MiB/strict UTF-8/allowlisted-header/1,000-row contract, canonicalizes financial evidence, deduplicates rows, and reconciles against local payment/refund ledgers. |
| Settlement import attempt/recovery | Implemented through the durable attempt ledger, ADMIN list/numeric detail, owner-scoped header recovery, and same-key no-reprocess rule. Import returns all bounded row errors and `omittedErrorCount=0`. |
| Settlement read API | Implemented through `GET /api/admin/payments/settlements` with status/source/base-date filters. |
| Missing-provider scan API | Implemented through `POST /api/admin/payments/settlements/reconcile`; defaults to 30 inclusive days, permits at most 90 days and 5,000 selected payments, returns the first 200 errors plus exact omitted count, and creates `PROVIDER_SETTLEMENT_NOT_FOUND` review rows only when no imported provider evidence exists for the order. |
| Settlement ignore API | Implemented through `PUT /api/admin/payments/settlements/{settlementId}/ignore`; marks rows `IGNORED` with an operator note and preserves auditability. |
| Settlement admin UI | Implemented in `/admin/payments` settlement tab with CSV import, summary, filters, row table, missing-provider scan, and ignore action. |
| Settlement audit | Import, generated reconciliation rows, and ignore actions create `payment_operation_audit_logs` rows. |
| State mutation boundary | Settlement operations do not mutate subscriptions, payment orders, finalized payments, refunds, billing agreements, or provider state. |

## Related Documents

- `deliverables/user/REQ-20260519-ATS-001.md`
- `deliverables/user/REQ-20260521-ATS-001.md`
- `deliverables/user/REQ-20260524-ATS-002.md`
- `deliverables/user/REQ-20260525-ATS-002.md`
- `deliverables/user/REQ-20260525-ATS-003.md`
- `deliverables/user/REQ-20260525-ATS-004.md`
- `deliverables/user/REQ-20260525-ATS-005.md`
- `deliverables/user/REQ-20260526-ATS-001.md`
- `docs/design/payment-integration-design.md`
- `docs/design/payment-refund-receipt-settlement-policy.md`
- `docs/design/payment-settlement-import-design.md`
- `docs/design/api-spec.md`
- `docs/design/db-schema.md`
- `docs/audit/p1-payment-integrity-closure-20260715.md`
- `docs/ui/screen-flow.md`
- `docs/ui/modal-list.md`
- `docs/SR/SR-92.md` - retired reference for the old inline one-time widget UX
