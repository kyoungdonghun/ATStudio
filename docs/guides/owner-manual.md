---
version: 1.0
last_updated: 2026-02-12
project: ATS
owner: MA
category: guide
status: stable
tier: 2
target_agents:
  - "*"
task_types:
  - documentation
---

# ATStudio 오너 매뉴얼 (Owner Manual)

> **목적:** ATStudio 프로젝트의 전체 구조를 이해하고, 각 디렉토리와 파일의 역할을 파악하여 효과적으로 에이전트에게 지시할 수 있도록 안내합니다.

---

## 1. 프로젝트 전체 지도

```
ATStudio/
│
├── 📋 CLAUDE.md                    ← [시작점] 에이전트 지침서 (모든 세션에서 자동 로드)
├── 📦 build.gradle                 ← Java 빌드 설정
│
├── 🤖 .claude/                     ← [에이전트 시스템] 멀티에이전트 엔진
│   ├── agents/                     ← 에이전트 정의 (11개)
│   ├── skills/                     ← 스킬 정의 (18개)
│   ├── config/                     ← 시스템 설정
│   └── scripts/                    ← 자동화 스크립트
│
├── ☕ src/                          ← [애플리케이션] 실제 서비스 코드
│   ├── main/java/                  ← Java 소스코드
│   ├── main/resources/             ← 설정파일, 템플릿
│   └── test/java/                  ← 테스트 코드
│
├── 📄 docs/                        ← [문서 체계] 규칙, 정책, 가이드
│   ├── standards/                  ← 헌법급 표준 (Tier 0)
│   ├── policies/                   ← 운영 정책 (Tier 1)
│   ├── architecture/               ← 시스템 설계 (Tier 1)
│   ├── guides/                     ← 실행 가이드 (Tier 2)
│   ├── templates/                  ← 문서 템플릿 (Tier 2)
│   ├── registry/                   ← 레지스트리 (Tier 2)
│   ├── project/                    ← ⭐ 당신의 기획 문서 (여기에 넣으세요)
│   ├── adr/                        ← 아키텍처 결정 기록
│   ├── design/                     ← 에이전트 설계
│   ├── analysis/                   ← 분석 문서
│   └── eval/                       ← 평가 문서
│
└── 📬 deliverables/                ← [산출물] 작업 결과물
    ├── user/                       ← 사용자 승인용 (REQ, WI 요약)
    └── agent/                      ← 에이전트 추적용 (WI 핸드오프, Evidence Pack)
```

---

## 2. 세부 구조 해설

### 2.1 CLAUDE.md — 에이전트 지침서

**무엇인가:** 모든 대화 세션에서 자동으로 로드되는 프로젝트 지침서.

**당신에게 어떤 의미인가:**
- 여기에 적힌 내용은 에이전트가 **매번** 읽습니다.
- 프로젝트의 기술 스택, 코딩 규칙, 워크플로우가 정의되어 있습니다.
- 변경하면 즉시 에이전트 동작에 영향을 줍니다.

**수정이 필요한 경우:** 기술 스택 변경, 새 규칙 추가, Phase 전환 시.

---

### 2.2 `.claude/` — 에이전트 시스템 (엔진)

당신이 직접 건드릴 일은 거의 없습니다. 에이전트 시스템의 내부 동작입니다.

```
.claude/
├── agents/                 ← 11개 에이전트의 역할 정의서
│   ├── ps.md               ← Product Strategist (REQ 작성)
│   ├── sa.md               ← Software Architect (설계)
│   ├── se.md               ← Software Engineer (구현)
│   ├── re.md               ← Reliability Engineer (테스트)
│   ├── pg.md               ← Privacy Guardian (보안)
│   ├── cr.md               ← Code Reviewer (코드 리뷰)
│   ├── eo.md               ← Ensemble Overseer (거버넌스)
│   ├── qa.md               ← Quality Assurance (빌드/품질)
│   ├── tr.md               ← Technology Researcher (기술 조사)
│   ├── uv.md               ← UX/UI Virtuoso (API 명세)
│   └── docops.md           ← Documentation Ops (문서 관리)
│
├── skills/                 ← 스킬 = 에이전트가 실행하는 도구
│   ├── create-req/         ← 요구사항 문서 생성
│   ├── create-wi-handoff-packet/  ← 작업지시서 생성
│   ├── build-check/        ← 빌드 검증
│   ├── test/               ← 테스트 실행
│   └── ... (18개)
│
├── config/                 ← 시스템 설정 파일
│   ├── workspace.json      ← 프로젝트 정보, 기술 스택, 라우팅
│   ├── context-injection-rules.json  ← 어떤 에이전트에게 어떤 문서를 주입할지
│   └── context-triggers.json         ← 키워드별 문서 자동 로드 규칙
│
└── scripts/                ← Python 자동화 스크립트
```

**알아둘 것:** `.claude/config/workspace.json`에 ATStudio의 기술 스택과 Phase 정보가 있습니다. Phase 2(React) 전환 시 이 파일의 `enabled` 플래그를 바꿉니다.

---

### 2.3 `src/` — 애플리케이션 코드

실제 ATStudio 서비스의 Java 소스코드입니다.

```
src/
├── main/
│   ├── java/com/atstudio/atstudio/
│   │   ├── config/         ← Spring 설정 (Security, Web, JPA 등)
│   │   ├── controller/     ← REST API 엔드포인트 (얇게 유지)
│   │   ├── dto/            ← 요청/응답 데이터 객체
│   │   ├── entity/         ← DB 테이블과 매핑되는 JPA 엔티티
│   │   ├── repository/     ← DB 접근 인터페이스
│   │   └── service/        ← 비즈니스 로직 (핵심)
│   │
│   └── resources/
│       ├── application.yml ← 앱 설정 (DB 연결, JWT 설정 등)
│       └── templates/      ← Thymeleaf HTML 템플릿 (Phase 1 UI)
│
└── test/java/              ← JUnit5 테스트 코드
```

**당신이 할 일:** 기능 요구사항을 에이전트에게 전달하면, `se`가 여기에 코드를 작성합니다.

---

### 2.4 `docs/` — 문서 체계

이것이 가장 복잡한 부분입니다. 역할별로 설명합니다.

#### Tier 0: 헌법 — `docs/standards/` (에이전트 전원 필수 로드)

```
docs/standards/
├── core-principles.md          ← 시스템 헌법 (모든 원칙의 최상위)
│                                  Section 13: ATStudio 도메인 원칙
│                                  Section 13.4: Technology Roadmap ← ⭐ Phase 정의가 여기 있음
│
├── development-standards.md    ← 개발 표준
│                                  Section 2A: Java/Spring Boot 코딩 표준
│                                  Section 2B: Python 스크립트 표준
│
├── documentation-standards.md  ← 문서 작성 표준 (메타데이터, 구조, 스타일)
├── glossary.md                 ← 용어 사전
│                                  Section 3-A: ATStudio 도메인 용어
│
├── evidence-pack-standard.md   ← Evidence Pack 작성 표준
├── evolution-pattern.md        ← 시스템 개선 절차
├── prompt-caching-strategy.md  ← 컨텍스트 캐싱 전략
└── glossary-sources.md         ← 용어 출처
```

#### Tier 1: 정책 — `docs/policies/`

```
docs/policies/
├── security-policy.md          ← 보안 정책 (JWT, MySQL 시크릿 관리) ⭐
├── execution-policy.md         ← 실행 승인 정책 (HITL 레벨)
├── quality-gates.md            ← 품질 게이트 (커밋 전 체크리스트)
├── access-control-policy.md    ← 접근 제어 정책
├── versioning-policy.md        ← 버전 관리 정책
├── template-governance.md      ← 템플릿 변경 거버넌스
└── future-policy-stubs.md      ← 미래 정책 스텁
```

#### Tier 1: 아키텍처 — `docs/architecture/`

```
docs/architecture/
└── system-design.md            ← 멀티에이전트 시스템 설계
```

#### Tier 2: 가이드 — `docs/guides/`

```
docs/guides/
├── development-workflow.md     ← ⭐ REQ→WI→위임 7단계 워크플로우
├── operation-process.md        ← 운영 프로세스
├── request-intake.md           ← 요청 방법 가이드
├── project-onboarding.md       ← 새 프로젝트 온보딩
├── traceability.md             ← 추적성 가이드
├── agent-docs-map.md           ← 에이전트별 필수 문서 맵
├── agent-facing-docs.md        ← 2-set 산출물 가이드
├── agent-evaluation.md         ← 에이전트 평가 가이드
├── eval-golden-set.md          ← 골든셋 평가
├── actor-guidelines.md         ← 역할별 행동 가이드라인
├── runbook-postmortem.md       ← 장애 대응/회고
└── owner-manual.md             ← ⭐ 이 문서
```

#### Tier 2: 템플릿 — `docs/templates/`

```
docs/templates/
├── adr-template.md             ← 아키텍처 결정 기록 템플릿
├── adr-example.md              ← ADR 작성 예시
├── db-design-template.md       ← DB 설계 템플릿 ⭐
├── interface-design-template.md ← API 인터페이스 설계 템플릿 ⭐
├── ia-design-template.md       ← IA(정보구조) 설계 템플릿
├── requirements-request-template.md ← 요구사항 요청 템플릿
├── project-request-template.md ← 프로젝트 요청 템플릿
├── impact-analysis-template.md ← 영향 분석 템플릿
├── wi-subagent-handoff-template.md ← WI 핸드오프 템플릿
├── domain-context-template.md  ← 도메인 컨텍스트 템플릿
├── project-workboard.md        ← 프로젝트 작업 보드
├── eval-report-template.md     ← 평가 리포트 템플릿
├── glossary-template.md        ← 용어 추가 템플릿
├── asset-promotion-checklist.md ← 자산 승격 체크리스트
├── context-request-template.md ← 컨텍스트 요청 템플릿
├── postmortem-template.md      ← 회고 템플릿
├── process-design-template.md  ← 프로세스 설계 템플릿
└── ma-session-kickoff-prompt.md ← 세션 시작 프롬프트
```

#### 레지스트리 — `docs/registry/`

```
docs/registry/
├── project-registry.md         ← 프로젝트 등록부 (PRJ-ATS-001)
├── asset-registry.md           ← 자산 등록부
├── context-registry.md         ← 컨텍스트 등록부
└── workboard.md                ← 작업 보드
```

#### ⭐ 프로젝트 문서 — `docs/project/` (당신의 기획 문서)

```
docs/project/                   ← 여기에 노션 문서를 넣으세요
├── (아직 비어있음)
│
│   권장 파일:
├── roadmap.md                  ← Phase 정의, 마일스톤, 우선순위
├── domain-model.md             ← 엔티티 관계, 핵심 개념
├── api-spec.md                 ← API 설계 (엔드포인트, 요청/응답)
├── db-schema.md                ← DB 스키마 (테이블, 컬럼, 관계)
├── user-flow.md                ← 사용자 흐름 (화면 전환, 시나리오)
├── feature-list.md             ← 기능 목록 (Phase별, 우선순위별)
└── business-rules.md           ← 비즈니스 규칙 (가격, 수수료, 라이선스)
```

#### 기타 디렉토리

```
docs/adr/          ← 아키텍처 결정 기록 (중요 기술 결정을 기록)
docs/design/       ← 에이전트 시스템 설계 (base-agent, 통신 프로토콜)
docs/analysis/     ← 분석 문서 (비어있음)
docs/eval/         ← 평가 문서 (비어있음)
```

---

### 2.5 `deliverables/` — 작업 산출물

에이전트가 작업하면서 자동으로 생성하는 문서입니다.

```
deliverables/
├── user/                       ← 당신이 확인하는 문서
│   ├── REQ-20260212-ATS-001.md ← (예시) 요구사항 정의서 → 당신이 승인
│   └── WI-20260212-ATS-001-summary.md  ← (예시) 작업 결과 요약
│
└── agent/                      ← 에이전트가 추적하는 문서
    ├── WI-20260212-ATS-001-handoff.md  ← (예시) 작업 지시서
    └── WI-20260212-ATS-001-evidence-pack.md ← (예시) 작업 증거
```

**흐름:**
```
당신의 요청
  → deliverables/user/REQ-*.md (당신이 승인)
    → deliverables/agent/WI-*-handoff.md (에이전트에게 전달)
      → 코드 작성 (src/)
        → deliverables/agent/WI-*-evidence-pack.md (작업 증거)
          → deliverables/user/WI-*-summary.md (당신에게 보고)
```

---

## 3. "Phase"는 어디에 정의하나?

### 현재 상태

Phase가 흩어져 있습니다:

| 위치 | 내용 |
|------|------|
| `CLAUDE.md` Tech Stack | "Thymeleaf → React 전환 예정" |
| `core-principles.md` Section 13.4 | Phase 1/2 기술 스택 테이블 |
| `context-injection-rules.json` | `react: enabled: false, phase_gate: "phase-2"` |

### 올바른 위치

**`docs/project/roadmap.md`**에 Phase를 정의해야 합니다.

이 문서가 없었기 때문에, Phase 정의가 여기저기 흩어진 것입니다.

**권장 구조:**
```markdown
# ATStudio Roadmap

## Phase 1: MVP (현재)
- 기간: 2026-02 ~
- 목표: 핵심 기능 동작
- 범위: 회원가입, 음악 CRUD, 검색, Thymeleaf UI
- 제외: 결제, React 전환

## Phase 2: Frontend 전환
- 목표: React SPA 전환
- 범위: React + TypeScript, REST API 분리
- 활성화: ESLint, Prettier, react-best-practices 스킬

## Phase 3: 고도화
- 목표: 결제, 정산, 추천 알고리즘
```

---

## 4. 당신의 액션 플랜

### 지금 해야 할 것

1. **`docs/project/roadmap.md` 작성** — Phase 정의 (가장 시급)
2. **노션 문서를 `docs/project/`에 정리** — 에이전트가 참조할 기획 문서
3. 기능 개발 시작 시 "음악 업로드 API 만들어줘"처럼 요청

### 문서별 에이전트 활용

| 당신이 넣는 문서 | 참조하는 에이전트 | 활용 |
|---------------|----------------|------|
| `roadmap.md` | ps, sa, eo | REQ 작성, 범위 결정, Phase 판단 |
| `domain-model.md` | sa, se | 엔티티 설계, JPA 구현 |
| `api-spec.md` | sa, se, uv | API 설계, Swagger 생성 |
| `db-schema.md` | sa, se | 테이블 생성, 마이그레이션 |
| `feature-list.md` | ps, sa | REQ 분할, WI 생성 |
| `business-rules.md` | se, pg | 비즈니스 로직 구현, 보안 검토 |
| `user-flow.md` | uv, se | UI 설계, Controller 설계 |

---

## 5. 자주 묻는 질문

### Q: 문서를 수정하면 에이전트가 바로 알아?
**A:** `CLAUDE.md`는 즉시 반영됩니다. `docs/` 파일은 에이전트에게 "이 파일 읽어"라고 지시해야 합니다. WI 핸드오프 패킷에 포함시키면 자동으로 전달됩니다.

### Q: `docs/project/`와 `deliverables/user/`의 차이는?
**A:**
- `docs/project/` = **입력** (당신이 작성한 기획 문서, 에이전트가 참조)
- `deliverables/user/` = **출력** (에이전트가 생성한 결과물, 당신이 승인)

### Q: 어떤 문서를 직접 수정해도 되나?
**A:**
- ✅ `docs/project/*` — 자유롭게 수정
- ✅ `CLAUDE.md` — 규칙 변경 시 수정 (에이전트에게 요청 권장)
- ⚠️ `docs/standards/*` — 에이전트에게 요청 권장 (메타데이터 동기화 필요)
- ❌ `.claude/*` — 에이전트에게 요청 (시스템 설정)
- ❌ `deliverables/*` — 에이전트가 자동 생성

### Q: Phase 전환은 어떻게 하나?
**A:** `docs/project/roadmap.md`에서 Phase를 업데이트하고, 에이전트에게 "Phase 2로 전환해줘"라고 요청하면:
1. `workspace.json`의 tech_stack 업데이트
2. `context-injection-rules.json`에서 react `enabled: true` 전환
3. `CLAUDE.md` Tech Stack 섹션 업데이트
4. ESLint/Prettier/React 스킬 활성화