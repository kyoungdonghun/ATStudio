---
version: 1.1
last_updated: 2026-07-15
project: ATS
owner: qa-integ
category: audit
status: stable
dependencies:
  - path: full-system-audit-20260713.md
    reason: Canonical P1 and conditional finding baseline
  - path: p0-release-blocker-closure-20260713.md
    reason: Current post-P0 implementation baseline
  - path: ../../deliverables/user/REQ-20260714-ATS-001.md
    reason: Approved remediation scope and WI plan
  - path: ../../deliverables/agent/WI-20260714-ATS-001-handoff.md
    reason: Traceability output contract
---

# P1 Remediation Trace Matrix - 2026-07-14

> Purpose: Freeze a current-code traceability baseline for `ATS020-P1-01` through `ATS020-P1-13` and bounded acceptance-environment items `ATS020-X-01`, `ATS020-X-02`, and `ATS020-X-04`. No finding is closed by this document.

## 1. Baseline and Evidence Rules

- Repository snapshot: `0ad4bc5` on `codex/p1-acceptance-hardening`.
- Current verdict: all 13 P1 findings remain open pending implementation and reproducible closure evidence.
- Evidence was inspected in current source, schema/manual patches, tests, designs, operations documents, and client documents. No application, schema, test, existing document, runtime log, live service, or database was modified.
- Existing tests are cited as current coverage. Names marked **required new** do not exist at this snapshot and are closure requirements, not executed evidence.
- Live Toss, SMTP, production MySQL, certification-document contents, raw tokens, billing keys, card data, and runtime logs were not inspected.

## 2. P1 Trace Matrix

| Audit ID | Confirmed current behavior and source evidence | Desired invariant and adjudication | Implementing WI | Verification proof required | Documentation target | Final review |
|---|---|---|---|---|---|---|
| `ATS020-P1-01` | `PlaylistService.createPlaylist` and `updatePlaylist` pass an arbitrary non-empty `MultipartFile` directly to `StorageService.store` (`PlaylistService.java:40-53,179-190`). `LocalStorageService` preserves the sanitized original filename and bytes (`LocalStorageService.java:51-70`); `/uploads/**` maps the storage root and is permit-all (`WebConfig.java:20-25`; `SecurityConfig.java:129-133`). Existing `PlaylistServiceTest` and `PlaylistControllerTest` contain no signature/decode/active-content cases. | Accept only bounded raster input, decode and re-encode to a canonical output, reject SVG/HTML/polyglot/MIME mismatch, and serve with fixed media/disposition controls. P1 is confirmed; same-origin executable delivery and P0 impact remain environment-conditional, not repository-proven. | `WI-009` implementation; `WI-019` security tests | **Required new:** playlist thumbnail security tests for valid raster, HTML/SVG/polyglot, spoofed MIME, decode limits, canonical output, and response headers. Run with `gradlew.bat test --tests "*Playlist*Security*"`. | `docs/design/api-spec.md` playlist endpoints; `docs/design/usecase/sound-playlist.md`; `docs/policies/security-policy.md`; client upload checks | `WI-024` PG review, `WI-025` integration review, final `WI-034` |
| `ATS020-P1-02` | Certification validation checks non-empty/count/extension/size only (`CompanyCertificationService.java:233-260`), stores `MultipartFile.getContentType()` (`:269-276`), and returns that value as download media type (`CompanyCertificationController.java:103-111,130-138`). Existing service/controller tests use nominal PDF bytes and verify role/download behavior, not authenticity. | Validate allowed signatures and detected MIME/parser result within bounded size, quarantine untrusted files, reject mismatch/polyglot input, and force safe authenticated download headers. Never treat extension or client MIME as trust evidence. | `WI-010`; `WI-019` | **Required new:** certification signature/MIME/parser/quarantine tests, disguised file cases, safe-download headers, and rollback cleanup. Run with `gradlew.bat test --tests "*CompanyCertification*Security*"`. | `docs/design/api-spec.md` Section 13; `docs/design/usecase/company-certification.md`; security policy; `docs/client/3-admin-checklist.md` | `WI-024`, `WI-025`, final `WI-034` |
| `ATS020-P1-03` | Server refresh uses the one stored refresh-token hash (`AuthService.java:75-110`). Password change and reset update only the password (`UserService.java:183-193`; `EmailService.java:142-159`); frontend logout only clears browser storage (`authStore.ts:49-59`), and `AuthController.java:21-74` has no logout endpoint. Current tests cover token rotation and password updates but not stale-token replay after termination. | Logout, password change, and password reset must atomically revoke the current refresh capability without exposing token material; replay must fail. Clarify whether one stored hash means single-session revocation or whether a session ledger is approved. | `WI-011`; `WI-019` | **Required new:** `AuthSessionRevocationIntegrationTest` covering logout/change/reset followed by old-refresh replay, including rollback behavior. Run with `gradlew.bat test --tests "*AuthSessionRevocationIntegrationTest"`. | Auth API contract, `docs/design/usecase/user-info.md`, security policy, client logout/password checks | `WI-024`, `WI-025`, final `WI-034` |
| `ATS020-P1-04` | Track, playlist, album, inquiry, and notice flows store before DB completion and inconsistently delete or retain old files (`TrackService.java:63-71,164-181`; `PlaylistService.java:39-53,178-190`; `AlbumService.java:42-50,113-120`; `QuestionService.java:49-66,173-188,207-223`; `NoticeService.java:43-59,92-132,159-173`). Delete failure is warning-only (`LocalStorageService.java:79-87`). Company certification is the only current reuse candidate with rollback-new/after-commit-old cleanup (`CompanyCertificationService.java:287-307`). | For each create/replace/delete path: remove new files after rollback, delete replaced files only after commit, preserve DB references on failure, record durable cleanup retry evidence, and make retries idempotent. Content authenticity remains P1-01/P1-02 scope; this row owns transaction/file convergence. | `WI-012`; `WI-019` | **Required new:** cross-domain transaction matrix for create/replace/delete, DB rollback, partial store failure, delete failure, retry, and idempotency. Run with `gradlew.bat test --tests "*FileLifecycle*IntegrationTest"`. | A unified file-lifecycle contract plus affected Track/Playlist/Album/Inquiry/Notice use cases and operations recovery guidance | `WI-024`, `WI-025`, final `WI-034` |
| `ATS020-P1-05` | Java emits settlement actions and target (`PaymentOperationAuditAction.java:17-19`; `PaymentOperationAuditTargetType.java:8`; `AdminPaymentSettlementService.java:105,217,250`; `PaymentOperationAuditLogService.java:145`). Fresh DDL omits them (`schema.sql:800-815`), and neither manual patch contains the missing ALTER. `AdminPaymentSettlementServiceTest` mocks persistence; H2 tests disable `schema.sql` (`src/test/resources/application.yml:1-7`). | Fresh DDL and an ordered existing-DB patch must accept every Java action/target, and settlement import/reconcile/ignore must flush and commit audit rows on MySQL. `ATS020-X-01` retained-DB proof remains separate. | `WI-004`; proof in `WI-018` and `WI-021` | **Required new:** `PaymentSettlementSchemaMySqlTest.allAuditEnumsFlushAndSettlementTransitionsCommit`; run on approved disposable MySQL with Hibernate `validate`. | `docs/design/db-schema.md`; settlement design/runbook; payment acceptance checklist | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-06` | `confirmBillingAgreement` is a default rollback transaction (`BillingAgreementApplicationService.java:161-178`). Provider/key/charge failure marks local state and throws `BusinessException` in the same transaction (`:176-182,222-226`). The current Mockito test observes mutated objects only (`BillingAgreementApplicationServiceTest.java:310-349`). | Persist one durable failed order/agreement outcome before returning the business failure; provider cleanup outcome must be separately recoverable and must not erase the attempt. | `WI-005`; `WI-018` | **Required new:** `BillingAgreementFailurePersistenceIntegrationTest.declinedInitialChargePersistsFailureAfterException`, reloading state in a new transaction. | Payment integration design, runbook, user-subscription use case, payment acceptance checklist | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-07` | Confirm and upgrade load without command locks; upgrade creates a random order and per-order key (`UserSubscriptionService.java:339-373`). `subscription_payments.payment_order_id` is nullable/non-unique (`schema.sql:518-538`). Correction: renewal now locks an agreement through `findByIDForRenewal` (`BillingAgreementRepository.java:37-40`; `RecurringRenewalService.java:99-106`), so the original statement of no renewal serialization is no longer wholly accurate. Unique finalization and stable command identity are still absent. | One logical confirm/upgrade/renewal command must have a stable idempotency identity, bounded lock/claim, one provider intent, and one unique local finalization. Keep batch isolation under P1-09. | `WI-006`; `WI-018` | **Required new MySQL concurrency:** concurrent confirm, duplicate upgrade intent, and two-worker one-period renewal tests; verify one provider call and one payment/final state. | Payment design, DB schema/index contract, runbook, acceptance checklist | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-08` | Renewal searches READY/IN_PROGRESS/FAILED/DONE (`RecurringRenewalService.java:48-52`) and `shouldReuseRenewalOrder` reuses every non-DONE order because of `status != DONE || samePeriod` (`:177-206`). Existing unit tests do not cover a new subscription with an old failed renewal order. | Renewal identity must include agreement, user-subscription lifecycle, and billing-period start. An old period/subscription attempt must never expire or suppress a fresh subscription. | `WI-007`; `WI-018` | **Required new:** `RecurringRenewalServiceTest.freshSubscriptionDoesNotReuseOldFailedRenewalOrder` plus period-boundary and replacement-subscription cases. | Payment design, user-subscription use case, runbook, payment acceptance checklist | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-09` | `processDueRenewals` is still one `@Transactional` method over all due IDs and provider calls (`RecurringRenewalService.java:84-117,157-173`). The P0 slice added deleted-user filtering and per-agreement row locking, but it did not create one transaction per agreement. | Enumerate/claim work without one batch rollback domain; process each agreement in an isolated transaction, persist/reconcile provider success before continuing, and ensure one agreement failure cannot roll back another. Multi-server scheduler locking is outside this WI unless separately approved. | `WI-007`; `WI-018` | **Required new:** `RecurringRenewalBatchIntegrationTest.failureOfSecondAgreementDoesNotRollbackFirstProviderSuccess`, with fake provider and committed reload assertions. | Payment runbook, integration design, acceptance checklist, deployment ownership note | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-10` | Refund create reads the source payment and aggregate reserved amount without locking it, then inserts a request (`AdminPaymentRefundService.java:89-111,248-262`). Only a later individual refund lookup is locked (`PaymentRefundRepository.java:45-57`). Existing tests are sequential Mockito cases. | Lock or atomically reserve against the source payment so concurrent requests cannot reserve more than the original finalized amount; retries remain idempotent. | `WI-008`; `WI-018` | **Required new:** `AdminPaymentRefundConcurrencyMySqlTest.concurrentReservationsCannotExceedOriginalAmount`. | Refund policy, DB schema, admin operations guide, payment acceptance checklist | `WI-023`, `WI-025`, final `WI-034` |
| `ATS020-P1-11` | Exported user/channel fields are quoted but formula-leading values are unchanged (`AdminWhitelistChannelService.java:127-157,171-198`). Existing tests verify columns/status changes, not `=`, `+`, `-`, or `@` payloads. | Neutralize spreadsheet formula-leading cells before CSV quoting while preserving display data and UTF-8/BOM behavior. Document that export is status-wide rather than keyword-filtered. | `WI-013`; `WI-019` | **Required new:** CSV tests for all formula prefixes, leading whitespace/control variants, quotes/newlines, ordinary values, and round-trip display. | `docs/design/api-spec.md` Section 12; whitelist use case; `docs/client/3-admin-checklist.md` | `WI-024`, `WI-025`, final `WI-034` |
| `ATS020-P1-12` | Fresh social callback receives tokens, then calls `fetchMe()` without the new access token before `authLogin` (`SocialLoginPage.tsx:42-59`). `fetchMe` only sets Authorization when passed a token (`frontend/src/api/auth.ts:99-103`). No `SocialLoginPage` callback test exists. | The callback must call authenticated profile retrieval with the returned token and commit a matching token/user pair atomically; failure must not leave half-authenticated storage. | `WI-014`; `WI-020` | **Required new:** `SocialLoginPage.test.tsx` with empty storage, returned-token Authorization, provider variants, profile-incomplete route, and failure cleanup. Run `npm test -- SocialLoginPage.test.tsx`. | Social auth API/use case, frontend auth contract, client social-login checklist | `WI-020` QA-FE, `WI-024`, `WI-025`, final `WI-034` |
| `ATS020-P1-13` | Payment documents still claim current delivery is complete (`docs/payment/known-limits-and-next-steps.md:25-39`) and acceptance lists expected flows without P1 launch gates (`docs/payment/acceptance-test-checklist.md:80-140`). Client guidance says to export CSV and download certification documents without formula/file-opening interim controls (`docs/client/3-admin-checklist.md:82-95`). Correction: the P0 slice added withdrawal cleanup procedures, so the older claim that withdrawal operations were undocumented is stale (`payment-operations-runbook.md:232-262`). | Current-state docs must say implemented but **not release-closed**, list P1 launch gates/evidence owners, and provide temporary safe export/file-review boundaries until controls land. Documentation follows verified implementation; it does not pre-close technical findings. | `WI-026`; client procedure `WI-027`; validation `WI-033` | Semantic cross-check against P1-01 through P1-12 evidence, documentation validator, claim scans, and `git diff --check`; final acceptance must link focused test/review evidence. | Payment design/runbook/pack, API/DB/use cases, security policy, `docs/client/*`, `SR-42` | `WI-025` input, `WI-033` mechanical validation, final `WI-034` |

## 3. Bounded Acceptance-Environment Matrix

| Audit ID | Repository evidence and uncertainty | Bounded exit evidence | WI / approval boundary |
|---|---|---|---|
| `ATS020-X-01` | Existing DB history is unknown. Only two manual patches exist; the older patch explicitly requires missing earlier payment migrations or rebuild (`20260615_align_payment_whitelist_schema.sql:18-23`). The certification patch creates metadata rows but has no legacy backfill/disposition (`20260618_company_certification_documents.sql:1-22`). Fresh DDL also has the P1-05 ENUM defect. Whether a target retained DB is affected cannot be inferred from the repository. | Inventory a copied DB; restore an ordered baseline/patch chain; resolve P1-05; backfill or explicitly disposition legacy certification files/rows; apply on disposable MySQL; pass Hibernate `validate` and focused flush tests. | `WI-004` and `WI-021` cover payment/schema proof. **Ownership gap:** no approved implementation WI explicitly owns legacy certification backfill/disposition; MA must assign it or revise scope before X-01 closure. Creating/deleting disposable DBs, applying DDL/data changes, or backfill requires separate approval. |
| `ATS020-X-02` | Rate-limit key uses `request.getRemoteAddr()` (`AuthRateLimitFilter.java:46`). Vite proxies `/api` and `/uploads` (`vite.config.ts:16-24`), but repository evidence cannot prove the effective client identity through Cloudflare. `allowedHosts: true` (`vite.config.ts:15`) is an adjacent acceptance hardening issue, not proof of X-02 impact. | Unit tests reject spoofed forwarded headers unless the immediate proxy is trusted; direct-local and Cloudflare acceptance prove distinct clients/accounts receive the intended keys and limits; Host allowlist remains explicit. | `WI-016` implementation/config tests; `WI-022` public smoke. Public URL sharing and live topology verification remain separately approved acceptance actions. |
| `ATS020-X-04` | Bootstrap is disabled by default but enabled solely by property, with no profile/startup guard (`TestUserBootstrapRunner.java:33-52`; `application.yml:87-90`). The shared example enables it and includes a known fallback password (`application-local.example.yml:49-56`). Repository state cannot prove any deployed environment enables it. | Require explicit non-production profile and external secret; production-like startup must fail when bootstrap is enabled; acceptance startup must prove the guard without logging the password. | `WI-015` acceptance profile/secret contract; focused backend proof in `WI-019`; public behavior in `WI-022` only after approval. |

## 4. Conflict and Dependency Adjudication

1. P1-07 is narrowed: renewal now has an agreement row lock, but confirm/upgrade locking, stable command identity, and unique finalization remain open.
2. P1-09 remains distinct from P1-07: it owns per-agreement transaction isolation, not command identity.
3. P1-04 owns file/DB convergence; P1-01 and P1-02 own content authenticity and serving controls.
4. P1-13 remains open, but the prior statement that withdrawal cleanup lacked an operations procedure is superseded by the P0 documentation update.
5. X-01 is not silently folded into P1-05. Fresh-schema ENUM repair does not prove a retained DB migration chain or legacy certification disposition.
6. No P2/P3 remediation is included. Adjacent Host, scheduler, and documentation-index concerns are referenced only where the approved acceptance path already names them.

## 5. Closure and Chain Rules

- No row may be closed from unit mocks, build success, or documentation wording alone.
- Domain review: payment P1-05 through P1-10 via `WI-023`; security/session/export P1-01 through P1-04 and P1-11/P1-12 via `WI-024`.
- Cross-layer review: `WI-025` must reconcile design, code, schema, tests, operations, and client wording.
- Current-state documentation follows review in `WI-026`/`WI-027`; quality gates are `WI-028` through `WI-033`; final closure decision is `WI-034`.
- Completion of WI-001 releases only its dependency edge. Phase 1 work still waits for both parallel design owners, `WI-002` and `WI-003`, to complete.

## 6. Current Payment Closure Appendix - 2026-07-15

This appendix does not rewrite the 2026-07-14 baseline above. It records the
later payment-integrity result at repository snapshot `14053e6`. The scope is
the current code/test gate for payment rows `ATS020-P1-05` through
`ATS020-P1-10`; retained-database, live-provider, deployment, client-acceptance,
non-payment, and overall REQ gates remain open.

| Audit ID | Current closure evidence | Current decision |
|---|---|---|
| `ATS020-P1-05` | The WI-023 reviewed baseline records aligned audit action/target ENUM DDL and focused static proof. Package A `WI-20260715-ATS-001` (`103fdf4`) preserves and extends the entity/schema/manual-patch contract. Package G `WI-20260715-ATS-007` (`830c8dd`) reports fresh schema PASS, Hibernate validate PASS, seven MySQL races PASS, and cleanup count `0`. | **CLOSED - current code/fresh disposable schema.** Retained-DB migration remains open. |
| `ATS020-P1-06` | Package B's impacted regression command includes `BillingAgreementFailurePersistenceIntegrationTest`; the command lifecycle persists provider outcomes before finalization. Independent payment/integration review completed after the follow-up corrections. | **CLOSED - current code/test gate.** Live Toss/deployment remain open. |
| `ATS020-P1-07` | Package B `WI-20260715-ATS-002` (`77c2ebd`) supplies stable commands and locked finalizers; WI-008 (`1ecfe5c`) closes completed-renewal convergence. MySQL races 1, 3, 4, and 7 prove one command/payment/final result under the specified interleavings. | **CLOSED - current code/test gate.** |
| `ATS020-P1-08` | `RecurringRenewalCommandIntegrationTest` proves two-date same-order/same-command retry; WI-011 (`46edd88`) consumes the retry scheduling gate on claim; MySQL races 1 and 2 prove exact period/attempt convergence. | **CLOSED - current code/test gate.** |
| `ATS020-P1-09` | Package B removes the broad renewal transaction and uses bounded per-command phases; Packages C/D/F use strict claim/provider/result boundaries. Focused and impacted regressions pass, and WI-012 reports no remaining P0/P1 in the reviewed follow-ups. | **CLOSED - current single-server code/test gate.** Multi-server ownership remains out of scope. |
| `ATS020-P1-10` | Package E `WI-20260715-ATS-003` (`f5bbd7b`) adds lease-aware refund recovery and exact reservation behavior; MySQL race 5 proves concurrent reservations never exceed the source payment, and race 6 proves stale-reclaimer fencing. | **CLOSED - current code/test gate.** Live refund execution remains unverified. |

The exact F-01 through F-05, package, correction, test, and MySQL artifact map is
maintained in [P1 Payment Integrity Closure](p1-payment-integrity-closure-20260715.md).

## Related Documents

- [Full-System Audit](full-system-audit-20260713.md): Canonical finding inventory.
- [P0 Closure Report](p0-release-blocker-closure-20260713.md): Current post-P0 baseline.
- [Approved REQ](../../deliverables/user/REQ-20260714-ATS-001.md): Approved scope and parallel work plan.
- [WI Handoff](../../deliverables/agent/WI-20260714-ATS-001-handoff.md): Output and evidence contract.
- [P1 Payment Integrity Closure](p1-payment-integrity-closure-20260715.md): Current payment code/test closure and remaining gates.
