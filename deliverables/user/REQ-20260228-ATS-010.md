[REQ]
REQ ID: REQ-20260228-ATS-010
Project: ATS
Owner: MA
Status: ✅ approved (2026-02-28)

[GOAL / WHY]
- Backend Audit (REQ-20260227-ATS-009) 에서 발견된 **CRITICAL 4건 + MAJOR 런타임/응답 오류 5건**을 수정한다.
- 프론트엔드 연동 전 서비스 안정성 확보가 목적이다.
- 수정 없이 프론트 연동 시 일반 사용자 프로필 접근 불가(CR-P-001), 구독 다운그레이드 시 환불 대신 추가 청구(CR-B-003), 트랙 필터 검색 시 런타임 오류(CR-A-001) 등 즉각적 장애가 예상된다.

[SCOPE]
- In:
  - **CR-P-001** (CRITICAL): SecurityConfig `/api/users/me` 명시 규칙 추가 — 와일드카드 ADMIN 규칙 앞에 선언
  - **CR-C-001** (CRITICAL): `Question` 삭제 시 `Answer` / `QuestionAttachment` cascade 처리
  - **CR-C-002** (CRITICAL): `AuthService`, `OAuth2Service` 클래스 레벨 `@Transactional(readOnly=true)` 추가
  - **CR-A-001** (CRITICAL): `Track` 엔티티에 `@OneToMany trackTags` 필드 추가 → `TrackSpecification` 런타임 오류 제거
  - **CR-B-001/002** (MAJOR): `UserSubscriptionController` DELETE 응답을 `ResponseEntity.ok()` → `ResponseEntity.noContent()` (204) 수정
  - **CR-B-003** (MAJOR): `UserSubscriptionService.changeSubscription()` `proratedAmount.abs()` 제거 → 음수(환불) 허용
  - **CR-B-004** (MAJOR): `SubscriptionService` `UserType.valueOf(userType)` → try/catch → 400 BAD_REQUEST 응답
  - **CR-C-003** (MAJOR): `UserService.updatePassword()` 현재 비밀번호 검증 로직 추가
  - 각 수정 항목에 대한 단위 테스트 추가/수정 (se 담당)
  - cr 코드 리뷰
  - re 회귀 검증 (전체 테스트 실행)
- Out:
  - CR-P-004 (JWT 시크릿 기본값): 의도적 제외 — 별도 결정 필요
  - MAJOR 2그룹 (N+1, URL검증, 소유권, TestController, RT만료 등): REQ-2 (후속)
  - MINOR / SUGGESTION 전체: REQ-2 또는 추후
  - DB 스키마 변경 (cascade ON DELETE): 서비스 레이어 처리로 대체

[SUCCESS CRITERIA (DoD)]
- [ ] CR-P-001: `GET /api/users/me`, `PUT /api/users/me`, `DELETE /api/users/me` — 일반 USER 역할로 200 응답 (403 아님)
- [ ] CR-C-001: `Question` 삭제 시 연관 `Answer`, `QuestionAttachment` 함께 삭제 — DataIntegrityViolationException 없음
- [ ] CR-C-002: `AuthService`, `OAuth2Service` `@Transactional(readOnly=true)` 클래스 레벨 적용 확인
- [ ] CR-A-001: `Track` 엔티티 `trackTags` 필드 존재 → `TrackSpecification.join("trackTags")` IllegalArgumentException 없음
- [ ] CR-B-001/002: DELETE 구독 취소 API → 204 No Content 응답 (200 아님)
- [ ] CR-B-003: 다운그레이드 시 `proratedAmount` 음수(환불) → `processPayment` 음수로 전달
- [ ] CR-B-004: 잘못된 `userType` 문자열 입력 시 500 아닌 400 BAD_REQUEST 응답
- [ ] CR-C-003: `updatePassword()` — 현재 비밀번호 불일치 시 에러, 일치 시 정상 변경
- [ ] 전체 테스트 통과 (기존 463건 + 신규 테스트 포함, 0 failures)
- [ ] cr 리뷰 PASS (CRITICAL/MAJOR 지적 없음)

[CONSTRAINTS / POLICIES]
- 작업 단위: WI
- REQ 승인 전 코드 수정 금지
- DB 스키마 변경 금지 — cascade는 서비스 레이어(수동 삭제) 또는 JPA cascade 옵션으로 처리
- 감사 보고서: `docs/audit/backend-audit-report.md` (근거 문서)
- Java 17, Spring Boot 4.x, JUnit5 표준 준수

[EXECUTION STRATEGY]
| Task | Subagent | Skill | Context Injection |
|------|----------|-------|-------------------|
| WI-034: Security/Auth/User 수정 (CR-P-001·C-002·C-003) | se | - | core-principles.md, development-standards.md |
| WI-035: Question cascade 수정 (CR-C-001) | se | - | core-principles.md, development-standards.md |
| WI-036: Track·Subscription 수정 (CR-A-001·B-001~004) | se | - | core-principles.md, development-standards.md |
| WI-037: 전체 테스트 회귀 검증 | re | /test | core-principles.md |
| WI-038: Security/Auth/User 코드 리뷰 | cr | - | core-principles.md, development-standards.md, security-policy.md |
| WI-039: Question/Track/Subscription 코드 리뷰 | cr | - | core-principles.md, development-standards.md |

[PARALLEL WORK PLAN]

> **목표**: CRITICAL 4건 + MAJOR 5건을 최소 시간에 수정하되, 도메인별 독립성을 이용해 최대한 병렬 처리.

| Phase | WI (병렬) | Agent | Depends On |
|-------|-----------|-------|------------|
| 1 | WI-034: Security/Auth/User ∥ WI-035: Question cascade ∥ WI-036: Track·Subscription | se ∥ se ∥ se | - |
| 2 | WI-037: 전체 테스트 회귀 검증 | re | Phase 1 전체 |
| 3 | WI-038: Security/Auth 리뷰 ∥ WI-039: Question/Track/Sub 리뷰 | cr ∥ cr | Phase 2 |

**WI 상세 범위:**

- **WI-034** (Security/Auth/User — 4 files):
  - `SecurityConfig.java` — `/api/users/me` 명시 규칙 (CR-P-001)
  - `AuthService.java` — `@Transactional(readOnly=true)` 클래스 레벨 (CR-C-002)
  - `OAuth2Service.java` — 동일 (CR-C-002)
  - `UserService.java` — 비밀번호 검증 추가 (CR-C-003)

- **WI-035** (Question cascade — 2~3 files):
  - `QuestionService.java` — `deleteQuestion()` 내 Answer, Attachment 명시 삭제 (CR-C-001)
  - `Question.java` — JPA cascade 옵션 검토/보완
  - 테스트: `QuestionServiceTest.java` cascade 시나리오 추가

- **WI-036** (Track + Subscription — 5 files):
  - `Track.java` — `@OneToMany(mappedBy="track") List<TrackTag> trackTags` 추가 (CR-A-001)
  - `TrackSpecification.java` — join 정상 동작 확인 (수정 불필요할 수 있음)
  - `UserSubscriptionController.java` — DELETE 204 응답 (CR-B-001/002)
  - `UserSubscriptionService.java` — `proratedAmount.abs()` 제거 (CR-B-003)
  - `SubscriptionService.java` — `UserType.valueOf()` try/catch (CR-B-004)

[QUALITY GATES]
| Gate | 조건 | 검증 방법 |
|------|------|----------|
| G1 | Phase 1 완료 | 3개 WI 모두 deliverable 제출 |
| G2 | 회귀 없음 | re: `./gradlew test` 전체 통과 (0 failures) |
| G3 | 리뷰 통과 | cr 2개 WI 모두 PASS (CRITICAL/MAJOR 지적 없음) |

[RISKS / OPEN QUESTIONS]
- **위험**: CR-C-001 Question cascade — `Answer`, `QuestionAttachment` repository 직접 삭제 시 순서 중요. FK 제약 위반 가능 → se가 삭제 순서 주의
- **위험**: CR-A-001 Track `trackTags` 필드 추가 시 기존 Track 조회 쿼리에 N+1 유발 가능 → `LAZY` fetch type 명시 필수
- 질문 없음 (감사 보고서 기준 명확)

[APPROVAL POINTS]
- 위 Scope 9개 이슈 수정 범위 확인 후 승인 또는 항목 조정 요청
- Phase 1 WI 3개 병렬 실행에 동의하는지 확인
