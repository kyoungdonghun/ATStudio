[REQ]
REQ ID: REQ-20260221-ATS-003
Project: ATS
Owner: MA
Status: ✅ approved

---

[GOAL / WHY]
- WI-013 코드 리뷰(cr)에서 발견된 Critical 1건 + Major 6건 + Minor 2건을 수정
- Critical C-01은 보안 defense-in-depth 위반 → 즉시 수정 필요
- Major N+1 쿼리 3건은 실사용 시 성능 저하 원인 → 사전 방어
- Major @Transactional 패턴 3건, ResponseDTO 1건은 ATStudio 코딩 표준 위반 → 정규화
- 구조 변경이 큰 minor m-01~06(ResponseDTO 위치, CustomUserDetails 파라미터)은 이번에 제외

---

[SCOPE]
- In:
  - **[C-01] 보안**: `TagController.createTag()`에 `@PreAuthorize("hasRole('ADMIN')")` 추가
  - **[M-01] N+1**: `LikeRepository.findAllByUser()`에 `@EntityGraph(attributePaths = "track")` 추가
  - **[M-02] N+1**: `DownloadQueueRepository.findAllByUser()`에 `@EntityGraph(attributePaths = "track")` 추가
  - **[M-03] N+1**: `PlayHistoryRepository.findAllByUserOrderByPlayedAtDesc()`에 `@EntityGraph(attributePaths = "track")` 추가
  - **[M-04] Transactional**: `UserService` 클래스 레벨 `@Transactional` → `@Transactional(readOnly = true)` 변경, mutating 메서드(`register`, `updateMyProfile`, `withdraw`, `completeProfile`, `updateUserByAdmin`)에 `@Transactional` 추가
  - **[M-05] Transactional**: `LikeService`, `DownloadQueueService` 동일 패턴 적용
  - **[M-06] ResponseDTO**: `NoticeController` 응답 3개(`createNotice`, `getNotice`, `updateNotice`)를 `ResponseDTO<NoticeResponse>` 래퍼로 감싸기
  - **[m-07] size 검증**: `PlayHistoryService.getMyHistory()` — `Math.max(1, size)` 추가
  - **[m-08] size 검증**: `NoticeService.getNotices()` — `Math.max(1, size)` 추가
  - 수정 후 전체 테스트 회귀 실행 (295개 이상 통과 확인)
- Out:
  - m-01~06 (ResponseDTO 서비스 → 컨트롤러 이동, CustomUserDetails → Long userID 파라미터 변경) — 구조 변경이 크고 테스트 수정 연쇄 발생 가능 → 별도 REQ
  - 새 기능 개발
  - 테스트 코드 추가 (기존 테스트 통과 여부만 확인)

---

[SUCCESS CRITERIA (DoD)]
- `TagController.createTag()`에 `@PreAuthorize("hasRole('ADMIN')")` 존재
- `LikeRepository`, `DownloadQueueRepository`, `PlayHistoryRepository` 각각 `@EntityGraph(attributePaths = "track")` 적용
- `UserService`, `LikeService`, `DownloadQueueService` 클래스 레벨이 `@Transactional(readOnly = true)`, 각 mutating 메서드에 `@Transactional` 적용
- `NoticeController` 3개 응답이 `ResponseDTO<NoticeResponse>` 래퍼 사용
- `PlayHistoryService`, `NoticeService` size 파라미터에 `Math.max(1, size)` 적용
- `gradlew.bat test` → 295개 이상 통과, 0 failures

---

[CONSTRAINTS / POLICIES]
- 작업 단위: WI
- REQ 승인 전 파일 수정 금지
- 보안/인가 관련 변경(C-01)은 최우선 처리
- 기존 테스트 통과 여부만 확인 (새 테스트 작성은 범위 외)

---

[EXECUTION STRATEGY]
| Task | Subagent | Skill | Context Injection |
|------|----------|-------|-------------------|
| C-01 보안 + M-06 ResponseDTO + m-07/08 size 검증 수정 | se | - | core-principles.md, development-standards.md |
| M-01/02/03 N+1 Repository @EntityGraph 수정 | se | - | core-principles.md, development-standards.md |
| M-04/05 Transactional 패턴 정규화 (3개 서비스) | se | - | core-principles.md, development-standards.md |
| 전체 빌드 + 테스트 회귀 검증 | qa | /test | core-principles.md, development-standards.md |

---

[PARALLEL WORK PLAN]

> Phase 1의 WI-014/015/016은 수정 대상 파일이 겹치지 않아 완전 병렬 실행 가능.

| Phase | WI (병렬) | Agent | Depends On |
|-------|----------|-------|------------|
| 1 | WI-014: C-01 보안 + M-06 ResponseDTO + m-07/08 size 검증 | se | - |
| 1 | WI-015: M-01/02/03 N+1 @EntityGraph 수정 | se | - |
| 1 | WI-016: M-04/05 Transactional 패턴 정규화 | se | - |
| 2 | WI-017: 빌드 + 전체 테스트 회귀 | qa | Phase 1 |

**수정 파일 분배:**
- WI-014: `TagController.java`, `NoticeController.java`, `PlayHistoryService.java`, `NoticeService.java` (4개)
- WI-015: `LikeRepository.java`, `DownloadQueueRepository.java`, `PlayHistoryRepository.java` (3개)
- WI-016: `UserService.java`, `LikeService.java`, `DownloadQueueService.java` (3개)

---

[QUALITY GATES]
| Gate | Condition | 검증 방법 |
|------|-----------|----------|
| G1 | Phase 1 완료 | 10개 파일 수정 완료 확인 |
| G2 | Phase 2 통과 | `gradlew.bat test` → 295개 이상 통과, 0 failures |

---

[RISKS / OPEN QUESTIONS]
- Risks:
  - `@EntityGraph` 추가 시 JPQL 쿼리와 충돌 가능성 낮음 (단순 findAll 메서드 대상)
  - `@Transactional(readOnly = true)` 변경 시 mutating 메서드 누락 위험 → se가 모든 메서드 검토 필수
  - `NoticeController` ResponseDTO 변경 시 기존 테스트 `status().isOk()` 확인 필요 (응답 구조 변경이나 테스트는 status만 확인하므로 영향 없을 것)
- Questions:
  - 없음 (범위 명확)

---

[APPROVAL POINTS]
- 승인: 위 범위 그대로 진행
- 변경: m-01~06도 포함하여 이번에 처리 (구조 변경 감수)
