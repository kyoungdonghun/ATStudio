[REQ]
REQ ID: REQ-20260818-ATS-002
Project: ATS
Owner: MA
Status: approved (사용자가 메인 화면의 두 문구를 명시적으로 변경 요청함)

[GOAL / WHY]

- 메인 화면 히어로 영역의 대상을 `쇼츠`와 외래어 `크리에이터` 대신 일관된 한국어 `창작자`로 표현한다.

[SCOPE]

- In:
  - 메인 화면 제목 `쇼츠를 위한 최고의 음악`을 `창작자를 위한 최고의 음악`으로 변경
  - 메인 화면 설명 `크리에이터를 위한 고품질 라이선스 음악.`을 `창작자를 위한 고품질 라이선스 음악.`으로 변경
  - 메인 화면 푸터 `쇼츠 크리에이터를 위한 음악 라이선스 플랫폼`을 `창작자를 위한 음악 라이선스 플랫폼`으로 변경
  - 메인 화면 집중 테스트와 프론트 정적 검사
- Out:
  - `docs/ui/mockup/`의 비활성 HTML 목업
  - 음원 상세의 사용 가능 콘텐츠 유형 설명에 포함된 `쇼츠`
  - 다른 화면, 레이아웃, 스타일, 라우팅, API 변경

[SUCCESS CRITERIA (DoD)]

- 메인 화면 히어로 제목과 설명에 `창작자를`이 표시된다.
- 메인 화면 푸터에 `창작자를 위한 음악 라이선스 플랫폼`이 표시된다.
- 대상 히어로 영역에서 기존 `쇼츠를 위한`과 `크리에이터를 위한 고품질` 문구가 더 이상 표시되지 않는다.
- 대상 푸터에서 기존 `쇼츠 크리에이터를 위한` 문구가 더 이상 표시되지 않는다.
- 다른 화면과 비활성 목업의 문구는 변경하지 않는다.
- 관련 테스트, TypeScript typecheck, ESLint, Prettier와 `git diff --check`가 통과한다.

[CONSTRAINTS / POLICIES]

- Work tracking unit: WI
- 현재 작업과 무관한 미추적 파일은 수정·스테이징하지 않는다.
- 활성 React SPA만 수정하고 비활성 목업은 유지한다.

[EXECUTION STRATEGY]
| Task | Subagent | Skill | Context Injection |
|------|----------|-------|-------------------|
| 메인 히어로 문구와 집중 테스트 변경 | se | react-best-practices, test, typecheck, eslint, prettier, create-wi-evidence-pack | core-principles.md, development-standards.md, frontend-standards.md |

[PARALLEL WORK PLAN]
| Phase | WI | Agent | Depends On |
|-------|----|-------|------------|
| 1 | WI-20260818-ATS-036: 메인 히어로 대상 문구 변경 및 검증 | se | - |

[QUALITY GATES]
| Gate | Condition | Verification |
|------|-----------|--------------|
| G1 | 정확한 히어로 두 문구와 푸터 한 문구만 변경 | HomePage diff와 문자열 검색 |
| G2 | UI 회귀 없음 | HomePage 집중 테스트 |
| G3 | 프론트 정적 품질 유지 | typecheck, ESLint, Prettier, diff check |

[RISKS / OPEN QUESTIONS]

- Risks:
  - 범위를 넓혀 비활성 목업이나 사용 맥락이 다른 `쇼츠`까지 바꾸면 의미가 달라질 수 있다.
- Questions:
  - 없음.

[APPROVAL POINTS]

- Approved:
  - 사용자가 히어로 두 대상 문자열과 푸터 문구를 `창작자` 표현으로 변경하도록 직접 지정했다.
