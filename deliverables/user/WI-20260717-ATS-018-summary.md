# WI-20260717-ATS-018 완료 요약

## 완료 결과

수용 테스트 실행 절차를 점검하는 계약 테스트 2개가 모두 통과했습니다. WI-019에서 실행 도구의 환경 변수 허용 목록을 보정한 뒤 최종 기동에도 성공했습니다.

최종 로그에서는 애플리케이션 기동 완료 1건과 QA 초기 데이터 준비 완료 1건이 확인됐습니다. 백엔드 오류, 데이터베이스 스키마 검증 오류, 프론트엔드 오류는 모두 0건이었습니다.

로컬과 공개 주소에서 프론트엔드와 API가 모두 HTTP 200으로 응답했습니다.

## 현재 실행 상태

| 항목 | 내용 |
|------|------|
| 실행 폴더 | `C:\Users\jm991\AppData\Local\ATStudio\acceptance-v1-final-20260717` |
| 공개 주소 | `https://protected-humor-michel-careful.trycloudflare.com` |
| 실행 중인 구성 | 백엔드, 프론트엔드, Cloudflare 터널 |
| 프로세스 소유권 | 현재 실행 폴더의 manifest가 관리하는 프로세스 |
| 로컬 프론트엔드와 API | 모두 HTTP 200 |
| 공개 프론트엔드와 API | 모두 HTTP 200 |

현재 실행 환경은 클라이언트가 확인할 수 있도록 계속 실행 중입니다.

## 세 번의 기동 경과

1. 첫 번째 기동은 오래된 외부 데이터베이스를 사용해 실패했습니다. 해당 데이터베이스에 `company_certification_audit_logs` 테이블이 없었습니다.
2. 두 번째 기동은 현재 `atstudio` 데이터베이스로 전환한 뒤 진행했습니다. 실행 도구의 V2 환경 변수 허용 목록이 빠져 있어 애플리케이션이 안전하게 기동을 차단했습니다.
3. WI-019에서 허용 목록을 현재 계약에 맞게 보정한 뒤 세 번째 기동이 성공했습니다. 현재 백엔드, 프론트엔드, 터널이 manifest 소유 프로세스로 실행 중입니다.

첫 번째 실패는 오래된 외부 데이터베이스 상태가 원인이었고, 두 번째 실패는 실행 도구와 현재 환경 계약의 불일치가 원인이었습니다. 두 원인은 모두 확인됐으며 최종 실행 로그에는 설명되지 않은 기동 오류가 남아 있지 않습니다.

## 역할별 API 결과

| 역할 | 확인한 조회 요청 | 결과 |
|------|------------------|------|
| 비로그인 사용자 | tracks, subscriptions | 모두 200 |
| 일반 사용자 | users/me | 200 |
| 일반 사용자 | user-subscriptions/me, admin/stats | 모두 403 |
| 구독 사용자 | user-subscriptions/me, licenses/me, whitelist-channels | 모두 200 |
| 유예 기간 사용자 | user-subscriptions/me | 200 |
| 사업자 사용자 | company-certifications/me, whitelist-channels | 모두 200 |
| 관리자 | stats, payment orders, admin whitelist, company certifications | 모두 200 |

일반 사용자가 구독자 전용 조회와 관리자 통계 조회에서 받은 403 응답은 권한 경계가 정상적으로 적용된 결과입니다.

## 브라우저 확인 결과

- AT.M 홈 화면이 정상적으로 표시됐습니다.
- 트랙이 없을 때의 빈 화면 안내가 정상적으로 표시됐습니다.
- 구독 화면에 요금제 6개가 정상적으로 표시됐습니다.
- 브라우저 콘솔 오류는 0건이었습니다.
- 오류가 아닌 React Router 향후 변경 경고만 확인됐습니다.

## 실행하지 않은 범위

외부 결제 제공자 호출, 결제 처리, 환불 처리, 관리자 변경 요청은 실행하지 않았습니다. 이번 결과는 대표적인 조회 흐름, 역할별 접근 권한, 화면 표시 상태를 확인한 것이며 실제 결제나 운영 환경의 배포 준비 완료를 뜻하지 않습니다.

비밀 정보, 데이터베이스 접속 정보, 비밀번호, 인증 토큰 값은 이 문서에 기록하지 않았습니다.

## 다음 승인 지점

클라이언트 확인을 위해 현재 공개 실행 환경을 계속 유지할지, 확인이 끝난 뒤 현재 manifest가 소유한 프로세스만 종료할지 결정해야 합니다.

외부 결제 제공자 호출, 결제·환불·관리자 변경 요청, 운영 배포 또는 운영 준비 완료 판단은 별도 범위와 승인이 필요합니다.
