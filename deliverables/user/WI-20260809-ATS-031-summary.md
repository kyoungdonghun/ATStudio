# WI-20260809-ATS-031 완료 요약

## 결론

WI-021부터 WI-030까지 발행된 독립 항목 136개를 빠짐없이 대조해 131개의
canonical root로 정리했습니다. 이번 WI는 결함을 수정한 작업이 아니라,
무엇을 바로 고칠 수 있고 무엇은 결정·보안 검토·추가 증거가 필요한지
분리한 문서 전용 통합 작업입니다.

제품 코드, 테스트, 브라우저, API, DB, Provider, 저장소, Git, 비밀정보,
의도적 ZIP에는 어떤 작업도 수행하지 않았습니다.

## 정량 결과

### 원본 항목

| 출처     | 항목 수 |
| -------- | ------: |
| WI-021   |       7 |
| WI-022   |      16 |
| WI-023   |      16 |
| WI-024   |      16 |
| WI-025   |      15 |
| WI-026   |      12 |
| WI-027   |      11 |
| WI-028   |      14 |
| WI-029   |      17 |
| WI-030   |      12 |
| **합계** | **136** |

136개 원본 항목과 source-to-root crosswalk를 각각 다시 세었으며, 누락과
중복은 모두 0입니다. 두 원본을 하나의 root로 묶은 사례는 5개입니다.
그중 3개는 동일 소유자·계약이 확인된 정확한 병합이고, 2개는 향후
검증 시 다시 분리될 수 있는 잠정 병합입니다.

### Canonical Root 분류

| 구분                                     |      수 | 의미                                                |
| ---------------------------------------- | ------: | --------------------------------------------------- |
| 명확한 결함 (`FIX-NOW`)                  |      98 | 현재 승인된 계약 안에서 수정 가능                   |
| 제품 정책 결정 필요 (`POLICY-GATE`)      |      14 | 동작을 선택하기 전 사용자 결정 필요                 |
| 보안 정책 결정 필요 (`SECURITY-GATE`)    |       3 | 사용자와 PG의 보안 결정 필요                        |
| 외부/실행 증거 차단 (`EXTERNAL/BLOCKED`) |       6 | 인증 세션, 실제 브라우저·Provider 등 추가 증거 필요 |
| 테스트 공백 (`TEST-GAP`)                 |       3 | 독립 결함 확정 없이 자동화 증거가 부족              |
| 문서 공백 (`DOC-GAP`)                    |       5 | 현재 구현·증거와 문서가 불일치                      |
| 확인된 통제 (`CONTROL`)                  |       2 | 수정하지 않고 회귀 통제로 유지                      |
| **합계**                                 | **131** |                                                     |

정규화 심각도는 `P0 0`, `P1 20`, `P2 88`, `P3 16`, `CONTROL 2`,
`BLOCKED 5`입니다. 확인된 P0는 없습니다.

## 명확한 결함과 P1 우선순위

사용자 정책 결정을 기다리지 않고 우선 처리할 수 있는 P1 결함은
14개입니다.

`CR-031-039`, `055`, `066`, `075`, `076`, `081-084`, `092`, `112-114`,
`121`

이 항목들은 WI-032부터 WI-041까지의 clear P1 범위에 배치했습니다.

다만 `CR-031-081`은 별도 주의가 필요합니다. 원본에서는 P0 후보였고,
통합 결과에서는 P1으로 유지했습니다. 화면이 0원 결제로 안내하는 동안
서버에서는 정상 가격 주문이 준비될 수 있지만, 실제 Provider 결제나 운영
영속 상태는 확인되지 않았기 때문입니다. 위험을 낮추어 보거나 실제 결제가
발생했다고 단정하지 않았습니다.

## 정책·보안 결정 게이트

P1이지만 바로 구현해서는 안 되는 root는 6개입니다.

| Root         | 먼저 필요한 결정                                               |
| ------------ | -------------------------------------------------------------- |
| `CR-031-054` | Track 비활성화 시 관계와 이력을 보존할지 제거할지              |
| `CR-031-093` | billing-key 정리 실패의 안전한 재시도 정책                     |
| `CR-031-104` | 첨부파일 형식·개수·크기 계약                                   |
| `CR-031-106` | 다운로드 성공을 권한 영속화와 바이트 전달 중 어디에서 확정할지 |
| `CR-031-115` | Settlement 파일 인코딩·형식·MIME·크기 범위                     |
| `CR-031-116` | 금액·통화·Provider ID의 영속 필드 범위                         |

전체 사용자 게이트는 22개이며 다음 5개 묶음으로 정리했습니다.

| 결정 묶음                     | 게이트 수 |
| ----------------------------- | --------: |
| 신원·개인정보·세션            |         4 |
| 제품 동작                     |         5 |
| 결제·다운로드 등 외부 효과    |         6 |
| 첨부파일·Settlement 증거 계약 |         5 |
| 데이터·fixture 변경 승인      |         2 |
| **합계**                      |    **22** |

어떤 선택도 WI-031에서 대신 결정하지 않았습니다. 미래 회계 export 수신자
결정은 현재 결함 수정을 막지 않으며, duration 정리와 Usage fixture 수정은
진단이 아니라 실제 데이터 변경 단계만 차단합니다.

## 차단된 증거

6개 root는 제품 결함으로 단정하기보다 추가 실행 증거가 필요합니다.

- 실제 키보드 Submit·Tab·Escape·focus 동작
- 복구 가능한 인증 사용자 Profile 세션
- 통제된 일회용 메일 링크
- 승인된 소셜 로그인 Provider 계정
- 인증 entitlement·미디어 오류·대규모 페이지 fixture
- 인증된 ADMIN 화면의 네 가지 목표 viewport

기존 duration 및 Usage 데이터 drift는 외부 증거 부족으로 분류하지
않았습니다. 수정 대상은 명확하지만, 실제 데이터 변경 전에 사용자 승인이
필요한 항목입니다.

## 확인된 통제

- `CR-031-006`: 한 번 재현되지 않은 Track timing 관찰은 결함으로 확정하지
  않습니다.
- `CR-031-105`: Question 첨부 권한은 Question 가시성 정책을 상속하며 현재
  증거에서는 별도 권한 결함이 아닙니다.

두 항목은 수정 WI를 만들지 않고 향후 회귀 검증의 통제로 유지합니다.

## 49개 WI의 의미

`WI-032`부터 `WI-080`까지 49개 WI를 제안했습니다. 이는 신규 기능 49개가
아닙니다. 결제, 인증/세션, 파괴적 보존, 바이너리 전달, Whitelist,
Settlement, 접근성처럼 위험 성격과 주 파일 소유권이 다른 작업을 작게
분리한 수정·결정·증거·테스트·문서·회귀 검증 포트폴리오입니다.

번호는 root 소유권을 안정적으로 묶기 위한 식별자입니다. 항상 번호 순서대로
실행한다는 의미는 아닙니다. 실행 순서는 심각도, 승인 준비 상태, 파일
소유권, 의존성을 따릅니다.

1. 먼저 clear P1인 WI-032부터 WI-041을 처리합니다.
2. 그 직후 필요한 입력이 준비되는 대로 P1 결정 패킷 WI-062와
   WI-064부터 WI-067을 열고, 일반 P2/P3 완료 전에 결론을 확보합니다.
3. 승인된 P1 구현은 새 handoff를 만든 뒤 안전한 의존성 위치에 우선
   삽입합니다. 이 미래 WI는 현재 49개 수에 포함하지 않았습니다.
4. WI-063 등 비P1 결정은 필요한 도메인 증거가 준비된 뒤 진행할 수
   있습니다.

131개 root는 각각 정확히 한 번만 다음 조치에 배정됐습니다. 누락, 중복,
범위 밖 배정은 모두 0입니다.

## 다음 안전 조치

다음 실행 대상은 `WI-032`입니다. 범위는 `CR-031-081`과
`CR-031-082`의 구독 결제 의도·대상 불일치 containment입니다.

WI-032 handoff를 만들기 전에 main이 반드시 확인할 사항은 다음과
같습니다.

1. `ATS-027-F01` / `CR-031-081`이 원본 P0 후보이면서 현재 P1 containment
   대상이라는 점을 명시적으로 에스컬레이션합니다.
2. 0원 안내 뒤 정상 가격 주문이 준비될 수 있는 경계를 PG와 QA-INTEG가
   검토합니다.
3. 실제 결제가 확인된 것은 아니므로 test Provider만 사용하고 실결제를
   발생시키지 않는 실행 경계를 다시 확정합니다.
4. WI-032에 6개 P1 결정 게이트를 섞거나 사용자 결정을 대신 넣지 않습니다.

이 확인 뒤 main이 표준 WI handoff를 생성하는 것이 다음 안전한 작업입니다.

## 종료 상태

### 문서 품질 검증

- handoff, consolidated findings, Evidence Pack, 사용자 요약을 대상으로 한
  최초 Prettier 검사는 exit `1`이었습니다. handoff는 이미 정상이었고,
  나머지 세 문서에 formatting이 필요했습니다.
- 세 문서에 Prettier `--write`를 실행한 결과는 exit `0`이었습니다.
  - Consolidated findings: `215ms`
  - Evidence Pack: `31ms`
  - 사용자 요약: `24ms`
- 네 문서 전체의 최종 Prettier 검사는 exit `0`이었으며, 모든 대상 파일이
  Prettier code style과 일치했습니다.
- 문서 validation은 exit `0`이었습니다. Tier 0, 내부 링크, traceability ID
  543개, 문서 index를 포함한 모든 검사가 통과했습니다.
- `git diff --check`는 exit `0`, 출력 없음이었습니다. 다만 이 검사는 tracked
  파일만 확인하며 WI-031 출력은 untracked 상태입니다. 해당 출력 문서는
  Prettier와 문서 validation이 직접 검사했습니다.

### 포트폴리오 재검산

Formatting 후 공백 허용 parser로 독립 재검산한 결과는 다음과 같습니다.

- Root를 직접 소유하는 WI 행: 46개
- No-action root: 2개
- 전체 배정: 131개
- 고유 root: 131개
- 중복: 0개
- 누락: 0개

### 실행하지 않은 작업

- 제품 테스트·빌드: 실행하지 않음. WI-031은 consolidation-only입니다.
- 브라우저·API·DB·Provider 작업: 실행하지 않음.
- Git mutation: 실행하지 않음.
- 비밀정보와 의도적 ZIP: 검사하지 않음.

최종 근거는
`deliverables/agent/WI-20260809-ATS-031-consolidated-findings.md`와
`deliverables/agent/WI-20260809-ATS-031-evidence-pack.md`에 있습니다.
