
> Historical public derivative, published in this working tree on 2026-09-09 by WI-20260909-ATS-018. Original dates, language, status, decisions and reported outcomes below are historical, not current approval or reverified recovery/production evidence. The original remains unchanged in private archive `v1-artifacts-20260909-084405`. See [A068: original identity, public SHA-256 and review](../../docs/registry/development-history-recovery-20260909.md#a068). Raw/local-only evidence pointers are lookup keys, not files supplied by a source checkout.

[REQ]
REQ ID: REQ-20260817-ATS-005
Project: ATS
Owner: PS
Status: approved (2026-08-17 사용자 상시 승인 적용)

[GOAL / WHY]

- `WI-20260817-ATS-005`의 P5 `FAIL`은 진단 구현의 오탐이다. 독립 무출력 메모리 교차검증은 `ConvertFrom-Json -AsHashtable`와 Hashtable `Contains`로 모든 필수 필드의 존재ㆍ타입ㆍnonblank 집계 조건이 모두 `true`임을 확인했다.
- DB 연결ㆍDDLㆍ데이터 접근은 없었고, 기존 Java 컴파일 준비성은 `PASS`다. 기존 패치는 계속 미적용 상태다.
- 과거 중단ㆍ오탐 WI 기록은 역사 기록으로 보존하고, 정확한 P5 선행 조건을 사용한 단일 최종 복구 WI만 수행한다.

[SCOPE]

- In:
  - `WI-20260817-ATS-007` 하나를 `qa-integ`에 위임한다. 동일한 사전 승인 비공개 번들을 메모리에서만 처리한다.
  - 첫 선행 predicate는 `ConvertFrom-Json -AsHashtable`와 Hashtable `Contains`를 사용한 정확한 P5 집계다. 필수 필드 전체의 존재ㆍ타입ㆍnonblank 결과는 각각 `P5_EXISTS_TRUE`, `P5_TYPE_TRUE`, `P5_NONBLANK_TRUE`의 세 boolean 코드만 기록한다.
  - 각 predicate는 독립 오류 경계로 검증하며 일반 `catch`로 상태를 합치지 않는다. 모든 선행 조건 통과 후에만 기존 패치 원본 감사, 저장소 외부 Java 러너의 단 1회 컴파일, JDBC 연결 1회, 대상 부재 확인 1회, 정확한 기존 패치 최대 1회, 사후 구조 델타 확인 1회를 순서대로 수행한다.
  - 임시 자산을 정리하고 `validate_docs.py`와 대상 REQ/WI 산출물의 `git diff --check`를 수행한다.
- Out:
  - 제품 코드ㆍ설정ㆍ기존 패치 원본ㆍ스키마 원본 변경, 비공개 번들의 생성ㆍ수정ㆍ복사ㆍ영속화, 과거 WI 변경, 대상ㆍ범위 확대.
  - 비밀값, 번들 식별자ㆍ경로, 필드명ㆍ값, 자격 증명, 연결 대상, 원시 예외ㆍ로그ㆍ명령 출력의 표시ㆍ기록ㆍ보존.
  - 재시도, 대체 입력, 추가 연결ㆍ검사ㆍSQLㆍDDLㆍ데이터 작업, 실패 후 수리ㆍ롤백ㆍ보상ㆍ삭제ㆍ`DROP`.

[SUCCESS CRITERIA (DoD)]

- P5가 첫 선행 predicate로 실행되고, 증거에는 `P5_EXISTS_TRUE`, `P5_TYPE_TRUE`, `P5_NONBLANK_TRUE`만 남는다. 비공개 값이나 집계 외 세부 정보는 남기지 않는다.
- 동일 번들은 전체 수명 동안 메모리 안에만 존재한다. predicate별 검증은 독립적이며 일반 `catch`가 여러 상태를 오탐 또는 미시작 상태로 합치지 않는다.
- 원본 감사와 외부 Java 러너 컴파일이 각각 한 번 성공한 경우에만 DB 단계가 시작된다.
- DB 단계는 연결 `1`, 부재 확인 `1`, 정확한 기존 패치 적용 최대 `1`, 사후 구조 델타 확인 `1`을 초과하지 않는다. 실제 실패, 불일치 또는 정리 실패 뒤에는 재시도하지 않는다.
- 과거 차단 WI는 변경하지 않고, 정리ㆍ문서 검증ㆍ범위 제한 `git diff --check`가 통과한다. 제품 파일은 변경하지 않는다.

[CONSTRAINTS / POLICIES]

- Work tracking unit: WI. 본 REQ는 사용자 상시 승인 범위에서 승인되었으며, 설계 결정이나 범위 추가를 포함하지 않는다.
- 비공개 번들, 자격 증명 및 파생 연결 정보는 메모리 전용이며 모든 산출물과 표준 출력에서 제외한다.
- P5 세 코드 외의 비공개 입력 관측값이나 오류 세부 정보는 기록하지 않는다. 각 predicate의 실패는 해당 단계의 비밀 안전 고정 코드로만 종료한다.
- DB 단계 전에는 DB 연결ㆍSQLㆍDDLㆍ데이터 작업이 `0`이다. DB 단계에서는 승인된 기존 패치 외 SQL을 실행하지 않는다.
- 이전 `WI-20260817-ATS-003`, `WI-20260817-ATS-004`, `WI-20260817-ATS-005` 및 참조된 `WI-20260817-ATS-006` 게이트는 역사적 중단ㆍ오탐 기록으로 보존한다.

[EXECUTION STRATEGY]

| Task | Subagent | Skill | Context Injection |
|------|----------|-------|-------------------|
| P5 정확성 선행 검증부터 단발 기존 패치 및 정제 증거까지의 최종 복구 | qa-integ | create-wi-handoff-packet, create-wi-evidence-pack, validate-docs | core-principles.md, development-standards.md, security-policy.md, quality-gates.md, evidence-pack-standard.md |

[PARALLEL WORK PLAN]

| Phase | WI | Agent | Depends On |
|-------|----|-------|------------|
| 1 | WI-20260817-ATS-007: P5 교차검증 기반 단발 최종 복구 | qa-integ | 독립 P5 교차검증 `true`, WI-002 컴파일 준비성 `PASS`, 기존 패치 미적용 |

단일 WI의 순차 실행이다. 병렬 실행ㆍ대체 경로ㆍ후속 WI는 없다.

[QUALITY GATES]

| Gate | Condition | Verification |
|------|-----------|--------------|
| G1 | 역사 상태 보존 | 과거 WI를 수정하지 않고, P5 `FAIL`을 진단 구현의 오탐으로만 기록 |
| G2 | 정확한 P5 선행 조건 | `ConvertFrom-Json -AsHashtable`와 Hashtable `Contains`로 세 boolean 코드만 기록 |
| G3 | 비공개 입력 및 predicate 격리 | 메모리 전용, predicate별 오류 경계, 일반 `catch` 없음, 비밀정보 미보존 |
| G4 | 단발 실행 한계 | 원본 감사 `1`, 외부 Java 컴파일 `1`, 연결 `1`, 부재 확인 `1`, 패치 최대 `1`, 사후 델타 `1` |
| G5 | 종료와 문서 품질 | 실제 실패 후 재시도 `0`, 임시 자산 정리, `validate_docs.py`, 범위 제한 `git diff --check` 통과 |

[RISKS / OPEN QUESTIONS]

- Risks:
  - 실제 선행 조건ㆍ원본 감사ㆍ컴파일ㆍDB 단계ㆍ델타 검증 중 하나라도 실패하면 패치는 미적용 또는 미확인 상태로 남으며, 이 REQ는 후속 조치를 승인하지 않는다.
- Questions:
  - 없음.

[APPROVAL POINTS]

- 승인 기록: 사용자의 상시 승인 범위 안에서 본 REQ는 `approved`다.
- 추가 승인 필요: 패치 내용ㆍ대상ㆍ비공개 번들 계약ㆍpredicateㆍ횟수ㆍ허용 DB 동작ㆍ실패 후 조치ㆍ제품 또는 DB 설계 변경이 필요한 경우.
