
> Historical public derivative, published in this working tree on 2026-09-09 by WI-20260909-ATS-018. Original dates, language, status, decisions and reported outcomes below are historical, not current approval or reverified recovery/production evidence. The original remains unchanged in private archive `v1-artifacts-20260909-084405`. See [A067: original identity, public SHA-256 and review](../../docs/registry/development-history-recovery-20260909.md#a067). Raw/local-only evidence pointers are lookup keys, not files supplied by a source checkout.

[REQ]
REQ ID: REQ-20260817-ATS-004
Project: ATS
Owner: PS
Status: approved (2026-08-17 사용자 상시 승인 적용)

[GOAL / WHY]

- 정확한 기존 패치는 계속 미적용 상태이며, DB 연결ㆍDDLㆍ데이터 변경은 모두 `0`이다. Java 컴파일 준비성은 이미 `PASS`다.
- 이전 래퍼의 `FAIL_CLOSED`는 비공개 번들의 불량 증거가 아니다. 서로 독립인 로드, JSON, 평탄 맵, 필수 필드, JDBC host/port/db 검사를 하나의 일반 `catch`가 포착해 `NOT_STARTED`/`FAIL_CLOSED`로 잘못 기록한 운영자 래퍼의 관측성 결함이다.
- 비공개 번들의 값을 노출하거나 DB에 접근하지 않는 진단으로 각 조건을 분리 판정한 뒤, 모든 조건이 통과할 때만 기존 패치를 단 한 번 실행한다.

[SCOPE]

- In:
  - `WI-20260817-ATS-005`: 동일한 기존 비공개 번들을 메모리에서만 진단하고, 다음 각 predicate의 정제 상태 코드를 독립적으로 산출한다: 파일 접근, JSON 파싱, 평탄 맵, 각 필수 필드의 타입 및 nonblank, JDBC 정규식, loopback host, port, DB name.
  - `WI-20260817-ATS-006`: WI-005의 모든 predicate가 `PASS`일 때만, 동일한 predicate 구현을 재사용한 최종 단발 패치 러너로 `scripts/database/patches/WI-20260809-ATS-068-user-consents.sql`을 실행한다.
  - WI-006은 컴파일 준비성의 기존 `PASS`를 전제하고, 연결 `1`회, 사전 조건 확인 `1`회, 정확한 기존 패치 `1`회, 사후 구조 델타 확인 `1`회만 수행한다.
  - 각 WI 완료 시 정제 산출물과 문서 검증을 수행한다: 임시 산출물 정리, `validate_docs.py`, 대상 REQ의 `git diff --check`.
- Out:
  - WI-005의 DB 연결, Java/컴파일러 사용, 제품 코드ㆍ설정ㆍDB 변경, 비공개 번들의 생성ㆍ수정ㆍ영속화, 대상 변경.
  - 값, 경로, 대상 상세, 오류 원문, 식별자, 자격 증명, 비공개 번들 정보의 출력ㆍ기록. PowerShell의 `$Host` 사용.
  - 재시도, 수리, 롤백, 보상, 삭제, `DROP`, 추가 SQL, 추가 연결, 범위 확대 및 WI-006 실패 후의 모든 DB 후속 작업.

[SUCCESS CRITERIA (DoD)]

- WI-005는 DB 연결 `0`, Java/컴파일러 사용 `0`을 유지하며, 나열된 모든 predicate에 대해 값 없이 정제 게이트 코드만 출력한다. 일반 `catch`로 서로 다른 predicate를 `NOT_STARTED` 또는 `FAIL_CLOSED`로 합치지 않는다.
- WI-005는 임시 산출물을 정리하고, 비공개 입력값ㆍ경로ㆍ대상 상세ㆍ오류ㆍ식별자를 어떠한 증거나 로그에도 남기지 않는다.
- WI-006은 WI-005의 모든 코드가 `PASS`일 때만 시작하며, 동일한 독립 predicate 구현을 사용한다. 하나라도 `PASS`가 아니면 DB에 연결하지 않고 종료한다.
- WI-006은 연결 `1`, 사전 조건 확인 `1`, 정확한 기존 패치 적용 최대 `1`, 사후 구조 델타 확인 `1`만 수행하며 재시도하지 않는다.
- 두 WI의 증거는 상태 코드, 횟수, 범위 판정, 객체 상태 및 델타 수만 기록한다. 문서 검증과 대상 REQ의 `git diff --check`가 통과한다.

[CONSTRAINTS / POLICIES]

- Work tracking unit: WI. 본 REQ는 사용자 상시 승인 범위에서 승인되었으며, 새 범위 또는 설계 판단을 추가하지 않는다.
- WI-005와 WI-006은 동일한 `qa-integ` 담당자의 순차 WI다. WI-006은 WI-005의 전체 `PASS` 외에는 시작 조건이 없다.
- 비공개 번들은 메모리 내에서만 취급한다. 값, 경로, 연결 대상, 예외 원문, 신원 및 자격 증명을 표시ㆍ보존ㆍ공유하지 않는다.
- WI-005에서 허용되는 출력은 정제 predicate 게이트 코드뿐이다. 임시 자산은 종료 시 정리한다.
- 이전 REQ의 단발성ㆍ실패 시 즉시 종료ㆍ재시도 금지ㆍ추가 DB 작업 금지ㆍ범위 비확대 제약은 계속 유효하다.
- 실제 실패, 사전 조건 불일치, 검증 불일치, 증거 정제 실패 또는 정리 실패는 즉시 종료한다. 이 REQ는 실패 후 조치를 승인하지 않는다.

[EXECUTION STRATEGY]

| Task | Subagent | Skill | Context Injection |
|------|----------|-------|-------------------|
| 비공개 번들의 무DBㆍ무Java 독립 predicate 진단 및 정제 증거 | qa-integ | create-wi-handoff-packet, create-wi-evidence-pack, validate-docs | core-principles.md, development-standards.md, security-policy.md |
| 전체 `PASS` 조건의 최종 단발 패치 실행 및 정제 델타 증거 | qa-integ | create-wi-handoff-packet, create-wi-evidence-pack, validate-docs | core-principles.md, development-standards.md, security-policy.md |

[PARALLEL WORK PLAN]

| Phase | WI | Agent | Depends On |
|-------|----|-------|------------|
| 1 | WI-20260817-ATS-005: 무DBㆍ무Java 비공개 번들 predicate 진단 | qa-integ | WI-20260817-ATS-004의 DB 연결ㆍDDLㆍ데이터 변경 `0`, 컴파일 준비성 `PASS` |
| 2 | WI-20260817-ATS-006: 전체 진단 `PASS` 조건의 최종 단발 기존 패치 실행 | qa-integ | WI-20260817-ATS-005의 모든 predicate `PASS` |

두 WI는 순차 실행이며 병렬 실행이나 대체 경로는 없다.

[QUALITY GATES]

| Gate | Condition | Verification |
|------|-----------|--------------|
| G1 | 과거 상태 분류 | 기존 패치 미적용, DB 연결ㆍDDLㆍ데이터 변경 `0`, 컴파일 준비성 `PASS`, 래퍼 관측성 결함을 비공개 번들 불량으로 재분류하지 않음 |
| G2 | 무DB 진단 경계 | WI-005의 DB 연결 및 Java/컴파일러 사용 `0`; `$Host` 미사용; 값 없는 predicate 코드만 산출 |
| G3 | predicate 관측성 | 로드부터 DB name까지 각 predicate를 독립 코드로 기록하고 일반 `catch`가 상태를 합치지 않음 |
| G4 | 실행 시작 조건 | WI-005의 모든 predicate `PASS`일 때만 WI-006 시작; 그 외에는 DB 연결 `0`으로 종료 |
| G5 | 단발 DB 경계 | 연결 `1`, 사전 조건 확인 `1`, 정확한 기존 패치 최대 `1`, 사후 델타 확인 `1`, 재시도 `0` |
| G6 | 증거ㆍ정리 | 값ㆍ경로ㆍ대상ㆍ오류ㆍ식별자 미보존, 임시 산출물 정리, `validate_docs.py`, 대상 REQ `git diff --check` 통과 |

[RISKS / OPEN QUESTIONS]

- Risks:
  - WI-005의 predicate가 하나라도 `PASS`가 아니면 기존 패치는 미적용으로 남는다. 본 REQ는 분석ㆍ수리ㆍ재시도를 승인하지 않는다.
  - WI-006의 실제 실패나 사후 델타 불일치는 즉시 종료하며 DB 후속 작업을 허용하지 않는다.
- Questions:
  - 없음.

[APPROVAL POINTS]

- 승인 기록: 사용자의 상시 승인 범위 안에서 본 REQ는 `approved`다.
- 추가 승인 필요: 패치, 대상, 비공개 번들 처리 방식, predicate, 실행 횟수, 허용 DB 동작, 실패 후 조치, 제품ㆍ설정ㆍDB 변경 또는 설계 변경이 필요한 경우.
