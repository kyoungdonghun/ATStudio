# Findings: WI-20260809-ATS-022

## Audit Boundary

- Baseline: `codex/v1-release-rehearsal-fixes` at `e343c20`.
- Scope: authentication and account-entry rows `AUTH-01` through `AUTH-06` and `MEM-04`.
- Evidence is sanitized. No password, token, browser storage value, secret, raw mail link, or personal address is recorded.
- No product/runtime/DB mutation was performed. Findings are audit results, not fixes.

## Product Findings

| ID             | Severity | Classification                        | Expected source                                                                                                                                      | Sanitized actual evidence                                                                                                                                                                                                                                                                                            | Adjacent scope and next verification                                                                                              |
| -------------- | -------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `F-UI-022-001` | MAJOR    | Duplicate-submit window               | `WI-20260809-ATS-022-handoff.md` pending-fence acceptance; `frontend/src/pages/auth/SocialCompleteProfilePage.tsx`                                   | `SocialCompleteProfile` awaits network-backed `validate()` calls before setting `loading=true`. A second submit can enter while validation is pending. By contrast, Signup sets loading before its `Promise.all` availability checks, so no Signup duplicate-submit defect was found.                                | Consolidate in WI-030; a remediation WI should fence the full submit transaction and add a delayed-validation regression test.    |
| `F-UI-022-002` | MAJOR    | Fail-open capability UX               | `docs/policies/security-policy.md` public-capability contract; `frontend/src/hooks/usePublicCapabilities.ts`; Login, Signup, and PasswordReset pages | Capability-hook failure produces `null` plus an error, while the pages interpret `null` with `?? true`. Password authentication UI therefore remains available when capability discovery fails. Backend `PasswordLoginPolicy` still enforces the setting, so this is misleading fail-open UX, not a security bypass. | Consolidate in WI-030; decide and test a fail-closed or explicit-unavailable UI state without weakening backend enforcement.      |
| `F-UI-022-003` | MINOR    | Invalid route-state handling          | `frontend/src/pages/subscriber/ProfilePage.tsx` tab query handling                                                                                   | An unsupported Profile tab query can select no rendered panel and leave the content area blank instead of normalizing to a valid default or visible not-found state.                                                                                                                                                 | Consolidate in WI-030; verify canonical fallback plus back/forward navigation.                                                    |
| `F-UI-022-004` | MAJOR    | Error/empty-state conflation          | `frontend/src/pages/subscriber/ProfilePage.tsx` subscription loading contract                                                                        | All subscription-load failures are rendered as the same "no subscription" state. Authorization, network, and server failures are therefore presented as authoritative absence.                                                                                                                                       | Consolidate in WI-030; preserve distinct loading, empty, unauthorized, and retryable-error states.                                |
| `F-UI-022-005` | MINOR    | Missing completed-profile route guard | `frontend/src/router/index.tsx`; `frontend/src/pages/auth/SocialCompleteProfilePage.tsx`; backend completion endpoint                                | A user whose profile is already complete can still enter `/complete-profile`; only the backend rejects the completion attempt. The backend remains authoritative, but the route permits a dead-end workflow.                                                                                                         | Consolidate in WI-030; verify guard behavior for anonymous, incomplete, complete, and stale-session identities.                   |
| `F-UI-022-006` | MINOR    | Error-guidance loss                   | `frontend/src/pages/auth/PasswordResetPage.tsx`; auth exception response contract                                                                    | The forgot-password catch path discards rate-limit and server-provided guidance and replaces it with generic UI feedback. Enumeration-safe generic success remains correct for accepted requests, but actionable failure guidance is lost.                                                                           | Consolidate in WI-030; preserve safe `429` retry guidance without revealing account existence.                                    |
| `F-UI-022-007` | MINOR    | Error-guidance loss                   | `frontend/src/pages/subscriber/ProfilePage.tsx`; password-update response contract                                                                   | The Profile password-update catch path ignores `response.data.message`, hiding bounded backend guidance for rejected updates.                                                                                                                                                                                        | Consolidate in WI-030; verify safe message mapping for current-password, validation, rate-limit, and server failures.             |
| `F-UI-022-008` | MAJOR    | Accessibility semantics               | `WI-20260809-ATS-022-handoff.md` accessibility acceptance; auth/account form components                                                              | Password labels lack matching `id`/`htmlFor`; auth/account status and error regions lack live-region semantics; member-type toggles expose no programmatic selection state. Visible controls work, but assistive technology does not receive equivalent naming, status, or selection information.                    | Consolidate in WI-030; verify accessible names, `aria-live`/alert behavior, keyboard operation, and selected-state announcements. |

## Documentation Drift

| ID             | Severity | Classification      | Expected source                                  | Sanitized actual evidence                                                                                                                                  | Adjacent scope and next verification                                                                                |
| -------------- | -------- | ------------------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `D-UI-022-001` | MAJOR    | Documentation drift | `INFO-001` in `docs/design/usecase/user-info.md` | The document says registration proceeds to Login/account-ready state. The frontend routes to `/email-verify`, and the backend initiates verification mail. | Route to documentation correction after policy decision `R-UI-022-002`; recheck UI, API, and mail wording together. |
| `D-UI-022-002` | MAJOR    | Documentation drift | `INFO-002` in `docs/design/usecase/user-info.md` | The `INFO-002` View My Info response field list omits `companyName`.                                                                                       | Route to documentation correction; verify request DTO and validation constraints before editing the contract.       |
| `D-UI-022-003` | MINOR    | Documentation drift | `INFO-015` in `docs/design/usecase/user-info.md` | The document describes modal plus toast feedback, while the implemented Profile experience uses tab navigation plus inline feedback.                       | Route to documentation correction after confirming the intended current UX.                                         |

## Review Decisions

| ID             | Severity | Classification                        | Expected source                                                                                               | Sanitized actual evidence                                                                                                                                                                                        | Adjacent scope and next verification                                                                                                        |
| -------------- | -------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `R-UI-022-001` | REVIEW   | Missing consent contract              | Signup acceptance and user-information contract                                                               | The acceptance matrix asks for consent coverage, but neither the product UI nor the documentation defines, controls, or persists consent. Audit cannot decide whether the current UI is complete.                | User decision required before product or documentation remediation: define the consent contract or explicitly declare consent out of scope. |
| `R-UI-022-002` | REVIEW   | Unverified-login policy inconsistency | `INFO-001`; signup/email-verification UI; `src/main/java/com/atstudio/atstudio/service/auth/AuthService.java` | Backend authentication allows `isVerified=false`, the UI directs new registrations to verification, and documentation says the account is ready after registration. These are three different policy statements. | User decision required: either permit unverified login and align UI/docs, or deny/restrict it and align backend/UI/docs.                    |

## Blocked Evidence

| ID             | Severity | Classification             | Expected source                                    | Sanitized actual evidence                                                                                                                                                                                                                    | Adjacent scope and next verification                                                     |
| -------------- | -------- | -------------------------- | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `B-UI-022-001` | BLOCKED  | Fixture/session boundary   | `AUTH-01`, `AUTH-06`, and `MEM-04` acceptance rows | Valid Login, authenticated Profile presentation, and bounded Profile mutation/restoration could not be executed without a user login and known restorable fixture state. Anonymous Profile and `/complete-profile` access redirected safely. | Carry to WI-030 with an approved login fixture and known original Profile values.        |
| `B-UI-022-002` | BLOCKED  | Controlled-mail boundary   | `AUTH-03` and `AUTH-04` acceptance rows            | Missing/invalid verification and reset states were exercised, but valid, expired, and reused mail-link states require approved receiver/link fixtures. No raw token or uncontrolled mail was used.                                           | Carry to WI-030 with sanitized one-time fixtures or approved local receiver evidence.    |
| `B-UI-022-003` | BLOCKED  | External-provider boundary | `AUTH-05` acceptance row                           | Malformed and unknown social callbacks failed closed. Live social OAuth was not started because provider authorization was outside the approved effect boundary.                                                                             | Carry to WI-030 only with an approved provider test identity and callback evidence plan. |

## Decision Request

User direction is required for `R-UI-022-001` and `R-UI-022-002`. No consent or unverified-login policy is inferred by this audit.
