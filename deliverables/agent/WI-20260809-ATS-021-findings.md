# WI-20260809-ATS-021 Findings

## Scope

This register contains only findings from the frozen-code, read-only browser
sweep at baseline `e343c20` on `codex/v1-release-rehearsal-fixes`. No product
code, runtime configuration, database row, external provider, mail delivery, or
download operation was changed.

## Confirmed Findings

| ID             | Severity | Classification                   | Matrix / invariant               | Expected source                                                      | Actual evidence                                                                                                                                                                                                                                                                                                             | Next verification                                                                                                                                                            |
| -------------- | -------- | -------------------------------- | -------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `F-UI-021-001` | P2       | Implementation defect            | `PUB-09`; error recovery         | `docs/design/usecase/user-notice.md`; localized API error contract   | `GET /api/notices/999999` returned localized `404 RESOURCE_NOT_FOUND`, while `/notices/999999` rendered only `Failed to load notice` with no retry or back action. Screenshot: `output/ui-ux-audit/20260809/WI-021/F-UI-021-001_PUB-09_VD_missing-notice.png`. Source: `frontend/src/pages/public/NoticeDetailPage.tsx:28`. | Preserve the safe backend message or map it to Korean UI copy, add a recovery action, and cover `404`, network failure, and retry separately.                                |
| `F-UI-021-002` | P2       | Implementation defect            | `G-SUB`; safe return invariant   | `docs/ui/screen-flow.md:21`; `ProtectedRoute` return-target behavior | Anonymous `/profile`, Checkout, BUSINESS, and ADMIN deep links preserved an encoded `returnTo`. Anonymous `/playlists` redirected to `/login` without one. Source: `frontend/src/router/SubscriberRoute.tsx:81`.                                                                                                            | Make the anonymous subscriber guard preserve the safe path and query, then verify successful Login resumes the original subscriber route and unsafe targets remain rejected. |
| `F-UI-021-003` | P3       | Accessibility defect             | `SH-01`; mobile menu close paths | Keyboard-equivalent close behavior in the approved acceptance matrix | At 390x844 the menu opened, closed through its button and overlay, and closed after a mobile navigation link. Pressing Escape did not close it. `Header` has no Escape listener. Source: `frontend/src/layouts/Header.tsx:233-243`.                                                                                         | Add an Escape close path without breaking overlay, route-change, or focus behavior; verify focus returns to the opener.                                                      |
| `F-UI-021-004` | P3       | Accessibility/localization drift | `SH-01`; Korean public shell     | Korean user-facing and assistive copy                                | The theme buttons expose `Switch to dark mode` and `Switch to light mode` as accessible names in an otherwise Korean shell. Source: `frontend/src/layouts/Header.tsx:62`.                                                                                                                                                   | Localize the accessible names and retain the current toggle-state semantics.                                                                                                 |

## Debt and Observations

| ID             | Severity | Classification                     | Evidence                                                                                                                                                                  | Disposition                                                                                                                               |
| -------------- | -------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `D-UI-021-001` | P3       | Dependency migration debt          | The browser console repeatedly showed only the React Router v7 `startTransition` future-flag warning. No uncaught runtime error was observed during the owned routes.     | Keep separate from user-visible defects; resolve with the router upgrade plan or explicit future-flag adoption after regression coverage. |
| `O-UI-021-001` | None     | Non-repeatable latency observation | The public `/tracks/3` page had no `h1` at the first 900 ms sample, but the same deep-link reload rendered the expected title after 700 ms and stayed on the exact route. | Do not classify as a defect from one sample. Recheck under the later loading/error/stale-response audit with request timing evidence.     |

## Blocked or Unconfirmed

| ID             | Classification           | Attempted evidence                                                                                                                                                | Why it is not a product finding                                                                                                                                                                                                                                                                         | Required follow-up                                                                                                                                                                                                    |
| -------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `B-UI-021-001` | Browser-input limitation | The desktop search input accepted `봄에`, but the in-app browser's Enter dispatch did not trigger implicit form submission. Tab dispatch also did not move focus. | `Header.handleSearch` constructs `/tracks?keyword=<encoded>&page=1`, and the focused Header suites passed 27 tests, including `fireEvent.submit`. The same automation surface failed to produce normal Tab traversal, so the browser result is not sufficient to label search or focus behavior broken. | Recheck with a physical-keyboard acceptance pass or a second browser runner that proves native key events. Keep the existing unit evidence but do not treat it as a replacement for real-browser keyboard acceptance. |

## Adjacent Scope

- Valid Notice attachment controls were observed, but no file was downloaded.
- Login, Signup, account mutation, subscriber content mutation, payment,
  provider, mail, CSV, and upload/download effects belong to later WIs.
- No finding in this register authorizes a product fix before the initial audit
  sequence is synthesized and classified.
