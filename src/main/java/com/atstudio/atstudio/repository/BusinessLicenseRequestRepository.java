package com.atstudio.atstudio.repository;

import com.atstudio.atstudio.entity.BusinessLicenseRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BusinessLicenseRequestRepository extends JpaRepository<BusinessLicenseRequest, Long> {
}
